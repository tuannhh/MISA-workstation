<script setup>
import MButton from '../../../components/mds/MButton.vue'; import MEmptyState from '../../../components/mds/MEmptyState.vue'; import MMobileTopBar from '../../../components/mds/MMobileTopBar.vue'; import MTag from '../../../components/mds/MTag.vue';
defineProps({ rows:{type:Array,default:()=>[]},canManage:Boolean,safeAreaStyle:Object }); const emit=defineEmits(['back','create','edit','delete']);
</script>
<template><section class="mds-mobile-app flex h-[100dvh] min-w-0 flex-col overflow-hidden bg-[var(--mds-bg)]" :style="safeAreaStyle"><MMobileTopBar title="Bộ từ khóa" @back="emit('back')"><template #actions><MButton v-if="canManage" variant="link" class="[&]:h-[var(--mds-mobile-touch-target)]" @click="emit('create')">Thêm</MButton></template></MMobileTopBar><main class="min-h-0 flex-1 overflow-y-auto"><div v-if="rows.length" class="divide-y divide-[var(--mds-border-light)]"><article v-for="row in rows" :key="row.id" class="mds-mobile-gutter-x py-3"><div class="flex items-start justify-between gap-3"><div class="min-w-0"><strong class="block text-[14px] leading-5">{{row.name}}</strong><p class="mt-1 truncate text-[13px] text-[var(--mds-text-secondary)]">{{row.include.join(', ')||'Chưa có từ khóa'}}</p></div><MTag :color="row.enabled?'success':'neutral'">{{row.enabled?'Bật':'Tắt'}}</MTag></div><div class="mt-2 flex gap-2"><MTag color="neutral">{{row.category}}</MTag><MTag color="neutral">{{row.queryType}}</MTag></div><div v-if="canManage" class="mt-2 flex justify-end gap-3"><MButton variant="link" class="[&]:h-[var(--mds-mobile-touch-target)]" @click="emit('edit',row)">Sửa</MButton><MButton variant="link" class="[&]:h-[var(--mds-mobile-touch-target)] text-[var(--mds-danger)]" @click="emit('delete',row)">Xóa</MButton></div></article></div><MEmptyState v-else title="Chưa có bộ từ khóa" description="Quản trị viên có thể thêm bộ từ khóa mới."/></main></section></template>
