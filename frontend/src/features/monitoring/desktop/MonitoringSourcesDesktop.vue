<script setup>
import MButton from '../../../components/mds/MButton.vue'; import MEmptyState from '../../../components/mds/MEmptyState.vue'; import MTag from '../../../components/mds/MTag.vue';
defineProps({ rows:{type:Array,default:()=>[]},canManage:Boolean }); const emit=defineEmits(['back','create','edit','delete']);
</script>
<template><section class="min-h-0 bg-[var(--mds-bg-page)] p-4"><div class="mx-auto max-w-[1100px] overflow-hidden rounded-lg bg-[var(--mds-bg)] shadow-[var(--mds-shadow-card)]"><header class="flex flex-wrap items-start justify-between gap-3 border-b border-[var(--mds-border-light)] px-5 py-4"><div><h1 class="text-[20px] font-semibold">Nguồn tin giám sát</h1><p class="mt-1 text-[13px] text-[var(--mds-text-secondary)]">URL được máy chủ kiểm tra SSRF và tự dò RSS trước khi lưu.</p></div><div class="flex gap-2"><MButton variant="neutral" @click="emit('back')">Tổng quan</MButton><MButton v-if="canManage" variant="primary" @click="emit('create')">Thêm nguồn</MButton></div></header><div v-if="rows.length" class="overflow-x-auto"><table class="w-full min-w-[760px] text-left text-[13px]"><thead class="bg-[var(--mds-bg-page)] text-[12px] text-[var(--mds-text-secondary)]"><tr><th class="px-5 py-3">Nguồn</th><th class="px-3 py-3">Loại</th><th class="px-3 py-3">Chế độ</th><th class="px-3 py-3">Trạng thái</th><th v-if="canManage" class="px-5 py-3"><span class="sr-only">Thao tác</span></th></tr></thead><tbody class="divide-y divide-[var(--mds-border-light)]"><tr v-for="row in rows" :key="row.id"><td class="max-w-[500px] px-5 py-3"><strong class="block">{{ row.name }}</strong><a :href="row.url" target="_blank" rel="noopener noreferrer" class="mt-1 block truncate text-[12px] text-[var(--mds-brand-600)]">{{ row.url }}</a></td><td class="px-3 py-3"><MTag color="neutral">{{ row.type }}</MTag></td><td class="px-3 py-3">{{ row.mode || '—' }}</td><td class="px-3 py-3"><MTag :color="row.enabled?'success':'neutral'">{{ row.enabled?'Bật':'Tắt' }}</MTag></td><td v-if="canManage" class="whitespace-nowrap px-5 py-3 text-right"><MButton variant="link" @click="emit('edit',row)">Sửa</MButton><MButton variant="link" class="ml-2 text-[var(--mds-danger)]" @click="emit('delete',row)">Xóa</MButton></td></tr></tbody></table></div><MEmptyState v-else title="Chưa có nguồn tin" description="Thêm nguồn sau khi đã kiểm tra tính hợp lệ và quyền quản trị." /></div></section></template>
