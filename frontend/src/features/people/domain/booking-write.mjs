import { PeopleApiError } from './people-api.mjs';

export const BOOKING_CREATE_FIELDS = Object.freeze(['content_type', 'title', 'amount', 'article_link', 'booked_date', 'publish_date', 'status', 'note']);
export function toBookingCreateDraft() { return Object.freeze({ content_type: '', title: '', amount: '', article_link: '', booked_date: '', publish_date: '', status: 'Đã đặt', note: '' }); }
export function toBookingEditDraft(record = {}) { return Object.freeze({ content_type: record.contentTypeValue || '', title: record.title === '—' ? '' : (record.title || ''), amount: record.hasAmount && record.amount != null ? String(record.amount) : '', article_link: record.articleLink || '', booked_date: record.bookedDateValue || '', publish_date: record.publishDateValue || '', status: record.status === '—' ? 'Đã đặt' : (record.status || 'Đã đặt'), note: record.note || '' }); }
export function validateBookingCreateDraft(draft) { const errors = {}; if (!String(draft?.title || '').trim()) errors.title = 'Tên booking không được để trống.'; if (!String(draft?.booked_date || '').trim()) errors.booked_date = 'Ngày đặt là bắt buộc.'; const amount = Number(draft?.amount); if (draft?.amount !== '' && (!Number.isFinite(amount) || amount < 0)) errors.amount = 'Số tiền phải là số không âm.'; return Object.freeze(errors); }
export function toBookingCreatePayload(draft, { personId, personName }) { const errors = validateBookingCreateDraft(draft); if (Object.keys(errors).length) throw new PeopleApiError({ status: 400, code: 'BOOKING_CREATE_INVALID', message: Object.values(errors)[0] }); const result = { subject_type: 'person', subject_id: Number(personId), subject_name: String(personName || ''), ...Object.fromEntries(BOOKING_CREATE_FIELDS.map((field) => [field, draft[field] === '' ? null : draft[field]])) }; if (result.amount !== null) result.amount = Number(result.amount); return Object.freeze(result); }
export function toBookingUpdatePayload(draft) { const errors = validateBookingCreateDraft(draft); if (Object.keys(errors).length) throw new PeopleApiError({ status: 400, code: 'BOOKING_UPDATE_INVALID', message: Object.values(errors)[0] }); const result = Object.fromEntries(BOOKING_CREATE_FIELDS.map((field) => [field, draft[field] === '' ? null : draft[field]])); if (result.amount !== null) result.amount = Number(result.amount); return Object.freeze(result); }
