export function adminUsersViewModel(payload){const roles=payload?.roles||{};return Object.freeze({rows:Object.freeze((payload?.rows||[]).map((row)=>Object.freeze({id:Number(row?.id),username:String(row?.username||''),fullName:String(row?.full_name||''),roleLabel:String(roles[row?.role]||row?.role||''),active:Boolean(row?.active),email:String(row?.email||''),notify:Boolean(row?.notify_opt_in)})))});}
function auditText(value){return value==null||value===''?'—':String(value);}
export function adminAuditViewModel(payload){return Object.freeze({rows:Object.freeze((payload?.rows||[]).map((row)=>Object.freeze({id:Number(row?.id),actor:auditText(row?.username),action:auditText(row?.action),entity:auditText(row?.entity),entityId:Number.isInteger(Number(row?.entity_id))?Number(row.entity_id):null,detail:auditText(row?.detail),timestamp:auditText(row?.ts)})))});}
