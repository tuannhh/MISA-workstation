export function adminUsersViewModel(payload){const roles=payload?.roles||{};return Object.freeze({roles:Object.freeze(Object.entries(roles).map(([value,label])=>Object.freeze({value,label:String(label)}))),rows:Object.freeze((payload?.rows||[]).map((row)=>Object.freeze({id:Number(row?.id),username:String(row?.username||''),fullName:String(row?.full_name||''),role:String(row?.role||''),roleLabel:String(roles[row?.role]||row?.role||''),active:Boolean(row?.active),email:String(row?.email||''),notify:Boolean(row?.notify_opt_in)})))});}
function auditText(value){return value==null||value===''?'—':String(value);}
export function adminAuditViewModel(payload){return Object.freeze({rows:Object.freeze((payload?.rows||[]).map((row)=>Object.freeze({id:Number(row?.id),actor:auditText(row?.username),action:auditText(row?.action),entity:auditText(row?.entity),entityId:Number.isInteger(Number(row?.entity_id))?Number(row.entity_id):null,detail:auditText(row?.detail),timestamp:auditText(row?.ts)})))});}
export function adminFieldVisibilityViewModel(payload){return Object.freeze({module:String(payload?.module||''),rows:Object.freeze((payload?.fields||[]).map((row)=>Object.freeze({field:String(row?.field||''),isPublic:row?.is_public===true?true:row?.is_public===false?false:null,status:row?.is_public===false?'Đang ẩn':'Theo PolicyEngine'})))});}
