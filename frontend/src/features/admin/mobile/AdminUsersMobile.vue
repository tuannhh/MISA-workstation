<script setup>import MEmptyState from '../../../components/mds/MEmptyState.vue';import MMobileTopBar from '../../../components/mds/MMobileTopBar.vue';import MTag from '../../../components/mds/MTag.vue';defineProps({rows:{type:Array,default:()=>[]},safeAreaStyle:Object});const emit=defineEmits(['back']);</script>
<template><section class="mds-mobile-app flex h-[100dvh] min-w-0 flex-col overflow-hidden bg-[var(--mds-bg)]" :style="safeAreaStyle"><MMobileTopBar title="Quản trị hệ thống" @back="emit('back')"/><main class="min-h-0 flex-1 overflow-y-auto pb-[var(--mds-mobile-safe-bottom)]"><div v-if="rows.length" class="divide-y divide-[var(--mds-border-light)]"><article v-for="user in rows" :key="user.id" class="mds-mobile-gutter-x py-3"><div class="flex items-start justify-between gap-3"><div><strong class="block text-[14px]">{{ user.fullName || user.username }}</strong><p class="mt-1 text-[13px] text-[var(--mds-text-secondary)]">{{ user.username }} · {{ user.email || 'Chưa có email' }}</p></div><MTag :color="user.active?'success':'neutral'">{{ user.active?'Hoạt động':'Khóa' }}</MTag></div><MTag class="mt-2" color="brand">{{ user.roleLabel }}</MTag></article></div><MEmptyState v-else title="Chưa có người dùng" description="Máy chủ chưa trả người dùng được phép xem."/></main></section></template>
