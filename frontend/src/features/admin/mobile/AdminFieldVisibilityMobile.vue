<script setup>
import MButton from '../../../components/mds/MButton.vue'; import MEmptyState from '../../../components/mds/MEmptyState.vue'; import MMobileTopBar from '../../../components/mds/MMobileTopBar.vue'; import MTag from '../../../components/mds/MTag.vue';
defineProps({rows:{type:Array,default:()=>[]},savingField:String,canEdit:Boolean,error:String,safeAreaStyle:Object});const emit=defineEmits(['back','tighten']);
</script>
<template><section class="mds-mobile-app flex h-[100dvh] min-w-0 flex-col overflow-hidden bg-[var(--mds-bg)]" :style="safeAreaStyle"><MMobileTopBar title="Hiển thị trường" @back="emit('back')"/><main class="min-h-0 flex-1 overflow-y-auto pb-[var(--mds-mobile-safe-bottom)]"><p v-if="error" role="alert" class="mds-mobile-gutter-x mds-mobile-gutter-y text-[13px] text-[var(--mds-danger)]">{{ error }}</p><div v-if="rows.length" class="divide-y divide-[var(--mds-border-light)]"><article v-for="row in rows" :key="row.field" class="mds-mobile-gutter-x py-3"><div class="flex items-center justify-between gap-3"><strong class="min-w-0 truncate text-[14px]">{{ row.field }}</strong><MTag :color="row.isPublic===false?'warning':'neutral'">{{ row.status }}</MTag></div><div class="mt-2 flex justify-end"><MButton v-if="canEdit&&row.isPublic!==false" variant="secondary" class="[&]:h-[var(--mds-mobile-touch-target)]" :loading="savingField===row.field" @click="emit('tighten',row)">Ẩn trường</MButton><span v-else class="py-3 text-[13px] text-[var(--mds-text-secondary)]">{{ row.isPublic===false?'Đã ẩn':'Chỉ xem' }}</span></div></article></div><MEmptyState v-else title="Chưa có trường để cấu hình" description="Máy chủ chưa trả dữ liệu cấu hình."/></main></section></template>
