const CONFIG = Object.freeze({
  sponsorship: Object.freeze({ label: 'Tài trợ / giải thưởng', fields: Object.freeze([{ key: 'title', label: 'Tên khoản / giải thưởng', required: true }, { key: 'type', label: 'Loại' }, { key: 'amount', label: 'Số tiền (VND)', type: 'number', confidential: true }, { key: 'event_date', label: 'Ngày diễn ra', type: 'date' }, { key: 'status', label: 'Trạng thái' }, { key: 'note', label: 'Ghi chú', multiline: true }]) }),
  gift: Object.freeze({ label: 'Quà tặng đối ngoại', fields: Object.freeze([{ key: 'gift_type', label: 'Loại quà', required: true }, { key: 'value', label: 'Giá trị (VND)', type: 'number', confidential: true }, { key: 'giver', label: 'Người/đơn vị tặng' }, { key: 'event_date', label: 'Ngày tặng', type: 'date' }, { key: 'occasion', label: 'Dịp tặng' }, { key: 'note', label: 'Ghi chú', multiline: true }]) }),
  association_fee: Object.freeze({ label: 'Hội phí', fields: Object.freeze([{ key: 'year', label: 'Năm', type: 'number', required: true }, { key: 'amount', label: 'Số tiền (VND)', type: 'number', confidential: true }, { key: 'due_date', label: 'Hạn đóng', type: 'date' }, { key: 'paid_date', label: 'Ngày đã đóng', type: 'date' }, { key: 'status', label: 'Trạng thái' }, { key: 'staff', label: 'Người phụ trách' }, { key: 'note', label: 'Ghi chú', multiline: true }]) }),
  benefit_usage: Object.freeze({ label: 'Quyền lợi hợp đồng', fields: Object.freeze([{ key: 'title', label: 'Nội dung sử dụng', required: true }, { key: 'used_date', label: 'Ngày sử dụng', type: 'date' }, { key: 'note', label: 'Ghi chú', multiline: true }]) }),
});

export function financialSchema(entity) { const schema = CONFIG[entity]; if (!schema) throw new TypeError('Loại khoản cơ quan không hợp lệ.'); return schema; }
export function financialDraft(entity, record = null) { const schema = financialSchema(entity); return Object.freeze(Object.fromEntries(schema.fields.map((field) => [field.key, record?.[field.key] == null ? '' : String(record[field.key])]))); }
export function financialPayload(entity, input = {}, { existing = null } = {}) { const schema = financialSchema(entity); const payload = {}; for (const field of schema.fields) { const value = String(input[field.key] ?? '').trim(); if (field.required && !value) throw new TypeError(`Nhập ${field.label.toLowerCase()}.`); if (field.confidential && existing && !Object.hasOwn(existing, field.key)) continue; if (field.type === 'number') { if (!value && !field.required) continue; const numeric = Number(value); if (!Number.isFinite(numeric)) throw new TypeError(`${field.label} phải là số hợp lệ.`); payload[field.key] = numeric; } else payload[field.key] = value; } return Object.freeze(payload); }
export const FINANCIAL_ENTITIES = Object.freeze(Object.keys(CONFIG));
