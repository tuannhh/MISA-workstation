<script setup>
import MButton from '../../../components/mds/MButton.vue'; import MEmptyState from '../../../components/mds/MEmptyState.vue';
defineProps({ overview:Object, range:Object, loading:Boolean }); const emit=defineEmits(['range']); const money=(value)=>new Intl.NumberFormat('vi-VN',{style:'currency',currency:'VND',maximumFractionDigits:0}).format(Number(value||0));
</script>
<template><section class="min-h-0 bg-[var(--mds-bg-page)] p-4"><div class="mx-auto max-w-[1200px] space-y-4"><header class="rounded-lg bg-[var(--mds-bg)] px-5 py-4 shadow-[var(--mds-shadow-card)]"><div class="flex items-start justify-between gap-4"><div><h1 class="text-[20px] font-semibold">Báo cáo</h1><p class="mt-1 text-[13px] text-[var(--mds-text-secondary)]">Số liệu tổng hợp do máy chủ tính và phân quyền.</p></div><div class="flex gap-2"><MButton v-for="item in [['month','Tháng này'],['quarter','Quý này'],['year','Năm nay']]" :key="item[0]" :variant="range.kind===item[0]?'primary':'neutral'" :loading="loading&&range.kind===item[0]" @click="emit('range',item[0])">{{ item[1] }}</MButton></div></div><p class="mt-3 text-[12px] text-[var(--mds-text-secondary)]">{{ range.from }} → {{ range.to }}</p></header><MEmptyState v-if="!overview" title="Chưa có dữ liệu báo cáo" description="Kiểm tra khoảng thời gian hoặc quyền xem báo cáo."/><template v-else><div class="grid gap-3 sm:grid-cols-2 lg:grid-cols-4"><article v-for="item in [[money(overview.grandTotal),'Tổng chi'],[money(overview.spend.budget),'Ngân sách kỳ'],[`${overview.network.press} / ${overview.network.people}`,'Cơ quan báo chí / Nhân sự'],[`+${overview.network.newPeople}`,'Nhân sự mới']]" :key="item[1]" class="rounded-lg bg-[var(--mds-bg)] p-4 shadow-[var(--mds-shadow-card)]"><strong class="text-[22px] leading-7">{{ item[0] }}</strong><p class="mt-1 text-[13px] text-[var(--mds-text-secondary)]">{{ item[1] }}</p></article></div><div class="grid gap-4 lg:grid-cols-3"><section v-for="item in [[money(overview.spend.total),`${overview.spend.count} booking`,'Chi booking'],[money(overview.events.total),'Theo chi phí sự kiện','Chi sự kiện'],[money(overview.fees.total),'Theo hạn đóng trong kỳ','Hội phí']]" :key="item[2]" class="rounded-lg bg-[var(--mds-bg)] p-5 shadow-[var(--mds-shadow-card)]"><h2 class="text-[15px] font-semibold">{{ item[2] }}</h2><strong class="mt-3 block text-[20px]">{{ item[0] }}</strong><p class="mt-1 text-[13px] text-[var(--mds-text-secondary)]">{{ item[1] }}</p></section></div><section class="rounded-lg bg-[var(--mds-bg)] shadow-[var(--mds-shadow-card)]"><header class="border-b border-[var(--mds-border-light)] px-5 py-4"><h2 class="text-[15px] font-semibold">Đầu mối cần chăm sóc</h2></header><MEmptyState v-if="!overview.careRisk.length" title="Không có rủi ro chăm sóc" description="Không có đầu mối vượt ngưỡng trong dữ liệu trả về."/><div v-else class="divide-y divide-[var(--mds-border-light)]"><article v-for="person in overview.careRisk" :key="person.id" class="flex items-start justify-between gap-4 px-5 py-3"><div><strong class="text-[13px]">{{ person.fullName }}</strong><p class="mt-1 text-[13px] text-[var(--mds-text-secondary)]">{{ person.orgName || 'Chưa có cơ quan' }} · {{ person.days >= 999 ? 'Chưa có tương tác' : `${person.days} ngày` }}</p></div><span class="text-right text-[12px] text-[var(--mds-text-secondary)]">{{ person.action }}</span></article></div></section></template></div></section></template>
