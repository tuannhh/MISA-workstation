# 01 — Phát hiện audit đã hội tụ

> Kết quả 5 vòng debate Claude ↔ Codex @ commit `088fbb2`. Mọi finding kèm `file:line` đã kiểm chứng. Nhãn: **Severity / Retrofit-tier / Target-gate**.

## A. Nhóm P0 / High — phải xử lý trước khi mở rộng

### F1 — Money policy bypass (read + write + file) · **High / A / browser-production — ĐÃ FIX (W1.POLICY/W1.FILE)**
Root cause: **3 cơ chế che tiền song song, không có nguồn sự thật chung** + write-guard gắn thiếu.
- Registry nhóm-mật chỉ đăng ký 3 trường tiền: `organization.membership_fee`, `sponsorship.amount`, `gift.value` — `server/rbac.js:66-73`.
- Tiền supplier/booking/event **không** trong registry, che ad-hoc bằng `maskMoney()` — `server/routes.js:52-56`, dùng ở `:982-992` (supplier quote/transaction).
- File đính kèm `/files/:id` **không** map nhóm tiền — `server/routes.js:466-478` (chỉ gác `kind==='id_doc'`).
- **Read bypass:** user thiếu `org_fee` tải PDF hợp đồng/báo giá qua `/files/:id` → đọc được số tiền đã bị che ở JSON. (Xác nhận bằng thí nghiệm HTTP cô lập của Codex.)
- **Write bypass:** user thiếu `org_fee` vẫn ghi/sửa trường tiền qua API — `POST /api/suppliers/:id/quotes`, `POST/PUT /sponsorships` (`:180-192`), fee/booking/event cost.
- **Guard đã có nhưng gắn thiếu:** `stripDisallowed()` (`:58-64`) chỉ được gọi ở `PUT /partners/:id` (`:168`) và `PUT /people/:id` (`:394`) = **2/42 create-update call site**. Bỏ sót CREATE (`POST /partners` `:156-163`, `POST /people` `:383-389`) và toàn bộ sponsorship. Static: `buildInsert` 20 call site, `buildUpdate` 22, `stripDisallowed` 2.
- Ngoài helper còn đường ghi raw: `budgets.amount` upsert (`:635-641`), attachment insert (`:243-249, 429-438, 938-944, 1049-1053, 1129-1133`).
- Read leak còn ở **projection/tổng hợp** (không phải cột vật lý): `bookings.total_amount` (`:606-609`), `events.total_cost` (`:1073-1080`), report aggregates (`:647-724`), award `partBudget/mediaCost/totalCost` (`:759-775`).
- `stripDisallowed` **xóa âm thầm** → client nhận 200 mà dữ liệu không lưu (bug integrity/UX, che tấn công).
- **Fix hội tụ:** 1 `DATA_POLICY_REGISTRY` + 1 `PolicyEngine` ở tầng service, **fail-closed 403** (không silent-strip); SQL helper chỉ nhận dữ liệu đã authorize. Xem 03-data-classification.md.
- **CẬP NHẬT sau inventory G0.3 (`07-route-catalog.md`):** blast radius rộng hơn 2 ví dụ ban đầu. Write-bypass xác nhận ở **10 nhóm route**: `POST/PUT /partners` (membership_fee), sponsorships, fees, gifts, `POST /budgets` (raw SQL, ngoài helper `buildInsert/buildUpdate`), `POST/PUT /awards` (cost), award_participations (budget), supplier_transactions (value), supplier_quotes (unit_price), event_costs (amount). Củng cố quyết định D1 — vá per-route chắc chắn sót, cần PolicyEngine 1 choke-point.
- Read-bypass xác nhận nguồn gốc **duy nhất**: `GET /files/:id` (`routes.js:467-478`) — phục vụ nội dung mọi loại attachment, chỉ gate `kind==='id_doc'`.
- **Resolution:** legacy mask/strip rải rác đã được thay bằng `PolicyEngine` + `policy-service` tại read/write choke point; 24/24 entity đã route-wire, aggregate che theo projection, và download file xét entity/owner/tier thay vì chỉ `kind`. Test mapping hiện là 150/150 route; F9 bổ sung cột tier server-derived cho attachment ở bản vá 2026-09-02.

### F2 — Session store = MemoryStore trên Cloud Run · **High / B / browser-production — ĐÃ FIX 2026-09-02**
- `server/app.js:17-22` (sửa lại 2026-08-25, trước ở `index.js:19-24` khi chưa tách `createApp()`) không khai báo `store` → MemoryStore; cookie thiếu `secure`; không regenerate session sau login (session fixation) — `server/auth.js`.
- Cloud Run tái chế container / cold-start / scale >1 → **user bị đăng xuất ngẫu nhiên mỗi lần redeploy**. Bug đang xảy ra.
- Fix: durable store (khuyến nghị bảng Cloud SQL sẵn có), `secure:true`, session regenerate, fail-fast nếu production thiếu `SESSION_SECRET` (`:20` đang có default `'misa-pr-dev-secret-change-me'`).
- **W1.7 (2026-08-30) đã sửa hardening:** `cookie.secure=true` khi `NODE_ENV=production` (+ `trust proxy`); `req.session.regenerate()` khi login; fail-fast `SESSION_SECRET`; rate-limit `/api/login`; audit `LOGIN_FAILED`/`LOGOUT`.
- **2026-09-02 durable fix:** `SqlSessionStore` thay hoàn toàn `express-session` MemoryStore bằng bảng `web_sessions` shared trên DB hiện có. Regression khởi tạo app instance A → login → tắt A → instance B đọc `/api/me` thành công; logout xoá row và cookie cũ trả 401. PASS trên SQLite/MySQL. DevOps chỉ còn cấu hình/vận hành DB+`SESSION_SECRET`, không còn là dependency kiến trúc cho session persistence.

### F3 — SSRF trong monitoring (metadata/internal) · **High / B / browser-production — CLOSED 2026-08-27**
- **Root cause cũ:** `server/monitor.js` `fetchText(url)`/`resolveLink(uri)` và `server/ai.js` `/award-extract` fetch URL tùy ý từ cấu hình user hoặc Gemini grounding; redirect không được kiểm lại per-hop. Rủi ro Cloud Run là gọi metadata endpoint để lấy service-account token.
- **Resolution:** `server/safe-fetch.js` là seam outbound duy nhất: chỉ HTTP(S), cấm credentials URL/hostname local và IPv4/IPv6 private-reserved; DNS phải trả toàn bộ địa chỉ an toàn; mỗi redirect được validate lại; body cap 2 MiB. Transport HTTP(S) kết nối tới **đúng IP vừa verify** qua custom `lookup`, vẫn giữ Host/SNI — đóng TOCTOU DNS-rebinding thay vì `dns.lookup()` rồi để `fetch(hostname)` resolve lần nữa.
- **Enforcement:** monitor, grounding link, tạo/sửa source và `/ai/award-extract` đều dùng seam/validate; URL bị chặn trả `400`, không ghi source/mention và không gọi Gemini.
- **Evidence:** `server/test/unit-safe-fetch.test.js` BR-SSRF-021..027 (scheme, private IPv4/IPv6, DNS rebinding, redirect); `unit-ssrf-security.test.js` BR-SSRF-010/011; HTTP integration R122/R139. `OUTBOUND_ALLOWED_HOSTS` là allowlist triển khai tùy chọn; khi được set, host và subdomain ngoài danh sách fail-closed.

### F4 — AI data governance · **High / A / browser-production — TECHNICAL CONTROLS CLOSED; O8 LEGAL EXTERNAL**
- Audio tương tác (giọng nói nội bộ) + dữ liệu Excel/partner có thể ra Gemini; monitoring chỉ gửi tiêu đề/link công khai (rủi ro thấp).
- Đã có registry/gateway egress, `AI_DISABLED`, deny-list, audit không payload, consent voice, redaction, upload allowlist và live synthetic contract 8/8 (F32). **Còn O8:** Security/Legal MISA phải duyệt chính sách cho dữ liệu thật; code không thể tự cấp thẩm quyền đó.

### F5 — Native-Mobile UI · **CODE SLICES ĐÃ CÓ; local acceptance do Owner, host/device là handoff DevOps**
- Nhận định Gate-0 “chỉ responsive web” chỉ còn đúng với **legacy `public/app.js`**. Các strangler slice hiện có composition mobile riêng trong `frontend/src/features/*/mobile/`, root `.mds-mobile-app`, `MMobileTopBar`, safe-area và host-adapter fake-native; chúng không dùng desktop shell co CSS.
- **Quyết định owner 2026-09-02:** nghiệm thu phần code/UI bằng Chrome local với fake-native/mobile emulation là đủ trong repository này. Khi bàn giao build, DevOps/AMIS tự cấu hình launcher/WebView/bridge và device acceptance ở môi trường MISA; không là blocker để tiếp tục code UI.
- Giới hạn trung thực: browser local chứng minh composition native và interaction web, **không chứng minh** lifecycle/permission/Back/Dynamic Type của thiết bị AMIS thật. Phần đó là handoff reference [`31-o3-amis-bridge-acceptance.md`](31-o3-amis-bridge-acceptance.md), không được ghi sai là đã test thiết bị.

### F6 — `public/app.js` monolith cản shared contract · **High / B / strangler đang tiếp tục**
- Legacy vẫn giữ phần catch-all: routing, HTML-string và API client nằm chung trong `public/app.js` (37 `innerHTML`, 105 call `api()`, 12 view legacy, ~2.889 dòng tại audit gốc). Đây là lý do một màn mới khó dùng lại đồng thời cho Desktop MDS và Native-Mobile.
- `frontend/src/App.vue` nay không còn chỉ là shell: resolver route chọn strangler feature khi bật flag/surface; mỗi slice mới tách `domain/`, `desktop/`, `mobile/` dùng chung API contract nhưng **không dùng chung page DOM**. Legacy vẫn hoạt động cho phần chưa tách, tránh big-bang rewrite.
- **Kế hoạch được owner cho tiếp tục:** hoàn thành F6 theo vertical slice, ưu tiên màn còn legacy có giá trị tác nghiệp cao; mỗi slice cần characterization → domain/API → Desktop MDS → Native-Mobile → runtime local/test rồi mới bỏ phần legacy tương ứng. Không đưa F6 vào một commit rewrite toàn bộ.
- **Tiến độ 2026-09-02:** Dashboard tổng quan, Partner List/Create/Detail (4 loại cơ quan), Supplier List/Create/Detail/direct records, Reports Detail, Reminder ICS và **Monitoring operations** đã rời legacy theo cấu trúc `domain/` + Desktop MDS + Native-Mobile riêng. Monitoring operations bao phủ runs/scan, competitors, campaigns/results/evaluation và settings; scan chỉ gửi ID query đang enabled, deep-link nạp lại `/api/me` để giữ permission affordance. Runtime local native đã xác nhận route operations có top bar, tab/task action 48px và không rơi về legacy shell; unit contract kiểm projection/allowlist/API path. Reminder tải `.ics` trong AMIS host vẫn là handoff DevOps, AI highlights/brief/card được owner hoãn. Phần còn lại trong repo chủ yếu là AI monitor cards và một số legacy fallback chưa có release-scope evidence; chưa gắn nhãn hoàn tất 100% F6.

## B. Nhóm P1 / Medium

### F7 — `Atomics.wait` khóa event loop (chỉ đường MySQL) · **Deferred bounded risk: target 50 users / browser+native**
- `server/mysql-sync.js:63` `Atomics.wait` chạy **main thread**; mọi `all/get/run` (`:81-90`) đi qua `_call` → mỗi query MySQL block cả event loop (tối đa 30s timeout `:7`).
- Cloud Run concurrency mặc định 80 → 1 query chậm đứng hình cả instance. Microbenchmark Codex (latency 25ms giả lập): p95 518ms @20 req, 1.291ms @50.
- **Bẫy:** đường SQLite (`node:sqlite`) **không** qua Atomics → dev thấy nhanh, prod MySQL serialize. Benchmark BẮT BUỘC pin `DB_CLIENT=mysql`.
- **Quyết định owner 2026-09-02:** quy mô cần phục vụ là 30 người PR MISA, lấy **50 người đồng thời** làm mức dư phòng; không mở W2.4 async-repository rewrite chỉ vì kiến trúc hiện tại. Đo stress liên tục tại `8904a4a` đạt 4,64 rps, error 2,53%, p50 9.040 ms, p95 12.730 ms (`perf-baseline/2026-09-02T06-48-41-905Z.json`) — đây là bằng chứng bottleneck, không phải PASS.
- **W2.3 owner-bounded profile PASS (2026-09-02, commit `993d300`):** 50 session độc lập (5 super_admin, 5 admin, 30 executor, 10 viewer), stagger thao tác đầu và think time đều 5–15s, workload read/write/report/file chỉ chọn endpoint role được phép. `2026-09-02T08-50-21-609Z.json`: 229 request, **error rate 0**, p50 880ms, p95 7.414s, p99 8.964s, 3.07 rps; mọi status theo nhóm là 200. Đủ ngưỡng owner “50 người tác nghiệp không lỗi”, **không phải SLO enterprise** và không xoá rủi ro kiến trúc Atomics/1 connection.
- Hành động hiện tại: giữ baseline, **không rewrite async ngay** theo quyết định owner. Chỉ mở W2.4 nếu MISA vận hành không chấp nhận độ trễ/budget này, quy mô thực vượt 50, hoặc profile tác nghiệp tương lai bắt đầu có lỗi.

### F8 — Gemini reliability + sampling params deprecated · **Medium / B — CLOSED phần reliability+capability-map (W1.9, 2026-08-30)**
- `server/gemini.js:8-21` `call()` dùng fetch trần: không timeout/AbortController, không retry, không circuit breaker (trớ trêu: `monitor.js` có AbortController, `gemini.js` không).
- `:29,38,49` gửi `temperature`; config pin `gemini-3.5-flash` (`server/config.js:19`). Gemini release note 2026-07-21 (Codex xác minh URL changelog): `temperature/top_p/top_k` **deprecated** — nhưng **chưa có live proof 3.6 luôn trả HTTP 400** (có thể bị bỏ qua âm thầm).
- Fix: aiGateway mỏng (timeout/retry/kill-switch/usage) + capability-map `supportsSamplingParams` để strip params theo model; giữ pin 3.5 tới khi golden eval thắng.
- **W1.9 (2026-08-30) đã sửa:** `call()` có `AbortController` timeout (`GEMINI_TIMEOUT_MS`, mặc định 30s); retry tối đa 3 lần cho 429/5xx (backoff `GEMINI_RETRY_BASE_DELAY_MS * attempt`, KHÔNG retry lỗi 4xx khác/timeout); `supportsSamplingParams(model)`/`buildGenerationConfig()` strip temperature/top_p/top_k cho model ngoài allowlist (`gemini-3.5-flash` hiện tại) — fail-safe mặc định false khi đổi model (W2.6) mà quên cập nhật allowlist. **Chưa làm (không thuộc batch này):** circuit breaker, usage/cost tracking, kill-switch `AI_DISABLED` + data-egress enforcement (thuộc `W1.AI-POLICY`, F4).

### F9 — Attachment thiếu cột phân loại · **Medium / A / browser-production — ĐÃ FIX 2026-09-02**
- `attachments.classification_tier` được thêm bằng migration idempotent và backfill lại mỗi boot: `person/portrait=Public`, `person/id_doc=Restricted`, còn lại `Confidential`; client không gửi/đặt tier.
- `policy-engine.js#attachmentPolicyFor()` là registry server-owned. Event chỉ nhận allowlist nhãn tài liệu, alias legacy `doc`/`Tài liệu` chuẩn hoá thành `Khác`; kind lạ trả `400 INVALID_ATTACHMENT_KIND` **trước Multer**, không tạo file/row mồ côi. Upload award/supplier/agreement/work-log/person cũng persist tier + `private` ceiling rõ ràng.
- Evidence: D13-017/018 và R095 chạy xanh cả SQLite/MySQL; migration/backfill được xác nhận bằng HTTP + DB assertion.

## C. Rủi ro thứ-tự-thực-thi (Tier A — vì dữ liệu prod đã sống)

### R1 — Backfill TRƯỚC khi flip fail-closed
Wave 1 thêm classification attachment + bật download/write gate fail-closed. File/tiền cũ trên prod chưa có nhãn. **Bắt buộc:** (1) migration backfill classification cho mọi attachment cũ theo kind/owner/route allowlist → (2) verify 0 hàng NULL → (3) mới flip gate. Nếu flip trước: hoặc khóa nhầm user, hoặc để hở đúng lỗ đang vá. Backfill idempotent + báo cáo số hàng.

## D. Việc lẻ dễ rơi

### F10 — Chuỗi giống credential trong `DEPLOY.md` · **P1 / — / all — CLOSED (placeholder removed)**
Chuỗi minh hoạ đã bị thay bằng chỉ dẫn Secret Manager/biến môi trường; lịch sử không chứng minh đó từng là credential sống. Nếu MISA xác minh ngược lại, phải rotate tại provider — không thể suy ra hay làm thay từ repo.

### F11 — RBAC drift 2-role vs banner 5-tài-khoản · **Low / — / browser-production — ĐÃ FIX (D13/W1.RBAC)**
Mô hình runtime đã hội tụ về `viewer`/`executor`/`admin`/`super_admin`, seed/login docs và ma trận quyền được kiểm bằng PolicyEngine + 150 route mapping; không còn banner demo 5 tài khoản trái với runtime.

### F12 — Schema/API mismatch: `event_id` không tồn tại trong allowlist ghi của booking · **Medium / B / browser-production — ĐÃ FIX 2026-09-02**
`event_id` đã có trong `B_COLS`; POST và PUT persist đúng cột migration sẵn có. Test R047/R048 xác nhận DB round-trip ở cả SQLite/MySQL. Bản vá chỉ khôi phục contract lưu liên kết; **không tự suy diễn** quy tắc aggregate/cost theo event mới, vì đó là nghiệp vụ riêng chưa được owner định nghĩa.

### F13 — Bộ nhắc việc/thông báo KHÔNG chạy được trên MySQL · **High / B / browser-production (sẽ chặn production tương lai) — ĐÃ FIX** (G1A.3 commit 4, phát hiện qua characterization; sửa ngay trong G1A.3 commit 5 theo yêu cầu owner; câu chữ "production" sửa lại theo Codex audit A4)
`server/scheduler.js:28` (cũ) dùng `WHERE ... AND (recipient_user_id IS ? OR recipient_user_id=?)` — cú pháp `IS ?` chỉ hợp lệ với driver SQLite (`node:sqlite` chấp nhận `IS` như so sánh tổng quát có xử lý NULL); MySQL chỉ cho `IS` đi với từ khoá `NULL/TRUE/FALSE/UNKNOWN`, không cho placeholder tham số → mọi lần gọi thực sự chạm nhánh này ném `ER_PARSE_ERROR` ("You have an error in your SQL syntax ... near '1 OR recipient_user_id=1)'").
- `scheduler.runOnce()` ném lỗi ngay tại vòng lặp đầu tiên có user opt-in + mốc nhắc đến hạn — nghĩa là **toàn bộ pipeline in-app notification + email nhắc việc không tạo được bản ghi nào** khi `DB_CLIENT=mysql`.
- `scheduler.start()` (`:73-74`) bọc `try/catch` chỉ `console.error` rồi tiếp tục lịch 6h/lần — lỗi bị nuốt hoàn toàn, không có alert/log tồn trữ nào cho vận hành thấy. Route thủ công `POST /api/reminders/run` (`routes.js:859-862`) không có `try/catch` riêng nên rơi vào error handler chung của `app.js:35-39` → trả `400 {error: "<SQL syntax error>"}`, lộ luôn thông báo lỗi SQL thô ra client.
- **Cloud Run hiện tại chạy `DB_CLIENT=mysql`, nhưng đây CHỈ LÀ MÔI TRƯỜNG TEST, chưa phải production thật** (owner xác nhận `13-deployment-runbook.md` §B — hạ tầng production MISA chính thức chưa tồn tại) — bug này đã ảnh hưởng môi trường test MySQL hiện tại (nếu đã từng có mốc nhắc + user `notify_opt_in=1` đến hạn từ khi deploy test, tính năng này chưa từng tạo ra thông báo nào ở đó trước khi có bản fix này) và **sẽ chặn production MySQL thật khi hạ tầng đó lên**, nếu không sửa trước.
- Phát hiện qua characterization G1A.3 commit 4 (`server/test/integration-reminders.test.js`, test `R060`). **Đã sửa trong G1A.3 commit 5** (owner yêu cầu fix ngay để Codex audit luôn, không đợi Wave riêng): xoá hẳn nhánh `IS ?` — cả 2 call site (`:46`,`:54`) luôn truyền `u.id` là số thật (không bao giờ NULL), nên vế `IS ?` là nhánh chết chưa từng cần tới; đơn giản còn lại `recipient_user_id=?` với 1 tham số (bỏ tham số trùng thứ 2). Test `R060`/`R057-059` không cần driver-aware nữa — pass 200 nguyên bản ở cả SQLite/MySQL. Verify: `test:security` 6/6, `test:integration:sqlite` 261 pass+6 skip, `test:integration:mysql` 266 pass+1 skip.

### F14 — `grandTotal` báo cáo tổng hợp tính SAI trên MySQL (nối chuỗi thay vì cộng số) · **High / B / browser-production (sẽ chặn production tương lai) — ĐÃ FIX** (G1A.3 batch reports-awards, phát hiện qua characterization R052; sửa ngay theo yêu cầu owner cùng cơ chế F13)
`server/routes.js:752` (cũ) `grandTotal: totalSpend.s + evTotal + feeTotal` — cả 3 giá trị đều lấy trực tiếp từ `SELECT COALESCE(SUM(...),0) s` (aggregate SQL). Driver `mysql2` trả kết quả `SUM()` (kiểu `DECIMAL` do MySQL suy ra, dù cột gốc là `BIGINT`) dưới dạng **string** theo mặc định (để không mất độ chính xác số lớn), trong khi `better-sqlite3` trả `number`. Toán tử `+` trên 3 string do đó thực hiện **nối chuỗi**, không cộng số — ví dụ 2 booking `300000`+`700000` cho ra `grandTotal = "300000700000"` thay vì `1000000`.
- Đây là tổng tiền hiển thị ở màn báo cáo tổng hợp cho lãnh đạo (`GET /api/reports`, dashboard chính) — sai lệch **âm thầm, không ném lỗi**, khác F13 (F13 ném lỗi rõ ràng, F14 trả dữ liệu sai mà trông như hợp lệ).
- Không chỉ `grandTotal` — `spend.total` (`totalSpend.s`), `events.total` (`evTotal`), `fees.total` (`feeTotal`), `spend.budget` (`budget.s`) đều cùng nguồn gốc (aggregate SUM), cùng bị sai type trên MySQL dù giá trị hiển thị riêng lẻ (không cộng) vẫn đúng chữ số.
- Cloud Run hiện tại chạy `DB_CLIENT=mysql` nhưng **chỉ là môi trường test** (như F13) — bug đã ảnh hưởng môi trường test này và sẽ chặn production MySQL thật khi hạ tầng đó lên, nếu không sửa trước.
- Phát hiện qua characterization batch "reports-awards" (`server/test/integration-reports.test.js`, test R052 chạy MySQL). **Đã sửa ngay** (owner yêu cầu, cùng cơ chế đã áp dụng cho F13): bọc `Number(...)` quanh 4 giá trị SUM (`totalSpend.s`, `budget.s`, `evTotal`, `feeTotal`) ngay tại điểm đọc kết quả query — sửa tận gốc kiểu dữ liệu thay vì chỉ sửa phép cộng, để mọi chỗ dùng lại các giá trị này (hiện tại và tương lai) đều nhận `number` đúng trên cả 2 driver. Test mới xác nhận `typeof === 'number'` cho cả 3 field + `grandTotal === spend.total + events.total + fees.total` đúng bằng số trên cả SQLite và MySQL.

### F15 — FK không được MySQL thực thi cho `award_participations.award_id` (nghi ngờ lan rộng toàn schema) · **P2 Medium / — / ĐÃ SỬA tận gốc — Claude 2026-08-30** (G1A.3 batch reports-awards, phát hiện qua characterization R067; PHẠM VI MỞ RỘNG theo Codex ONE-SHOT AUDIT `f40be9c^..c0270a1`)
**ĐÃ SỬA (2026-08-30) — root cause xác nhận + sửa tận gốc, không phải vá triệu chứng từng route:**
MySQL/InnoDB **PARSE nhưng ÂM THẦM BỎ QUA** cú pháp `REFERENCES` gắn trực tiếp vào cột (inline
column-level, như SQLite chấp nhận) — hành vi đã tài liệu hoá của MySQL, MySQL chỉ tạo FK thật khi
có `CONSTRAINT ... FOREIGN KEY (col) REFERENCES tbl(col)` tách riêng (out-of-line). Xác nhận thực
nghiệm trực tiếp (không đoán): `SHOW CREATE TABLE award_participations` trước khi sửa chỉ còn
`award_id bigint DEFAULT NULL`, không còn REFERENCES/CONSTRAINT nào dù `db.js` khai đủ. `server/
mysql-sync.js`'s `translate()` (áp dụng khi dịch DDL SQLite -> MySQL cho mọi `CREATE TABLE`) nay
tách mọi cột `col TYPE REFERENCES tbl(refCol) [ON DELETE action]` inline thành `CONSTRAINT
fk_<table>_<col> FOREIGN KEY (col) REFERENCES tbl(refCol) [ON DELETE action]` out-of-line — xác
nhận đã tạo đủ **24/24 FK thật** trên MySQL qua `information_schema.KEY_COLUMN_USAGE` (khớp đúng số
lượng dòng `REFERENCES` trong `db.js`). Không có bảng nào tham chiếu tới bảng chưa được tạo (đã rà
thủ công thứ tự khai báo `CREATE TABLE` trong `db.js`), nên không cần hoãn/2-pass ALTER TABLE thêm
constraint sau khi tạo bảng. 5 route characterization từng ghi nhận lệch driver (R007/R067/R075/
R083/R091) nay hội tụ đúng 1 hành vi (400 khi FK gốc không tồn tại; cascade xoá đúng theo `ON DELETE
CASCADE`/`SET NULL`) — cập nhật lại test bỏ nhánh `isMysql ? ... : ...`, xem `gate1-test-mapping.md`
+ test mới `unit-mysql-sync-translate.test.js` (`BR-FK-001..007`). Full regression: SQLite 692/8
skip, MySQL 699/1 skip, security 6/6, mapping 281 rows PASS, `verify-g0.mjs` PASS.
`server/db.js` khai `award_id INTEGER REFERENCES awards(id) ON DELETE CASCADE` cho bảng `award_participations`. `POST /api/awards/9999999/participations` (award_id không tồn tại) trả **400** trên SQLite (FK constraint chặn insert) nhưng trả **200** trên MySQL (insert thành công, tạo participation "mồ côi") — route không tự kiểm tra award tồn tại trước khi insert, hành vi phụ thuộc hoàn toàn vào driver có/không thực thi FK.
- **Codex xác nhận độc lập và mở rộng phạm vi:** MySQL không chỉ bỏ qua FK lúc INSERT — **DELETE `awards` cũng KHÔNG cascade xoá `award_participations` liên quan** (dù schema khai `ON DELETE CASCADE`), để lại participation mồ côi **vĩnh viễn** (không chỉ tạm thời lúc insert sai award_id). Đây là dấu hiệu FK constraint có thể **không thực sự được tạo** trên MySQL cho quan hệ này (không phải chỉ `FOREIGN_KEY_CHECKS` tắt tạm thời lúc 1 câu lệnh), và khả năng ảnh hưởng **các FK khác trong schema**, chưa kiểm hết.
- **Đã có bằng chứng mạnh tại tầng DDL/migration, chờ inventory toàn schema** (cập nhật theo Codex ONE-SHOT AUDIT batch "suppliers"): xác nhận cùng hiện tượng lặp lại ở **ít nhất 4 quan hệ FK độc lập** (`award_participations.award_id`, `supplier_quotes.supplier_id`, `supplier_transactions.supplier_id`, `supplier_contacts.supplier_id`) — đủ mẫu để loại trừ khả năng ngẫu nhiên/lỗi cục bộ 1 bảng, chỉ về hướng nguyên nhân hệ thống ở cách `mysql-sync.js` dịch `REFERENCES ... ON DELETE CASCADE` sang MySQL DDL (constraint có thể không thực sự được tạo, hoặc `FOREIGN_KEY_CHECKS` tắt trong luồng migrate/seed) — chưa xác định chính xác nguyên nhân, việc đó thuộc phạm vi inventory Wave 1 dưới đây, không điều tra thêm trong các batch characterization (đúng scope-freeze).
- Rủi ro: dữ liệu mồ côi tích luỹ trên MySQL (môi trường test/production tương lai); không mất dữ liệu thật, không phải P0/P1 vì participation mồ côi không tự hiển thị gây sai lệch nghiệp vụ trực tiếp (khác F14) — nhưng phạm vi rộng hơn ban đầu tưởng.
- Test hiện tại (`server/test/integration-awards.test.js`, R067) đã characterize đúng cả 2 driver bằng nhánh `isMysql ? 200 : 400`, không che giấu khác biệt.
- **Xác nhận thêm ở batch "suppliers" (`server/test/integration-suppliers.test.js`, R075/R083, KHÔNG phải điều tra mới — chỉ đo lại đúng phạm vi Codex đã cảnh báo khi các route trong batch tiếp theo tình cờ chạm cùng pattern FK):** cùng hiện tượng lặp lại ở bảng khác — `supplier_contacts.supplier_id` INSERT với `supplier_id` không tồn tại: SQLite 400, MySQL 200 (R075). Xoá `suppliers` có `supplier_quotes`/`supplier_transactions`/`supplier_contacts` liên quan: SQLite cascade xoá đúng theo `ON DELETE CASCADE`, MySQL để lại cả 3 bảng con mồ côi — xác nhận bằng query trực tiếp qua `db` sau khi cha đã xoá (R083), không chỉ suy luận qua response HTTP. Củng cố nghi ngờ ban đầu: đây là vấn đề **hệ thống ở tầng schema/migration MySQL**, không phải lỗi cục bộ 1 bảng.
- **Xác nhận thêm ở batch "events-dashboard" (`server/test/integration-events-dashboard.test.js`, R091, cùng cơ chế đo — không mở rộng điều tra):** xoá `events` có `event_costs` liên quan — SQLite cascade đúng, MySQL để lại `event_costs` mồ côi, xác nhận bằng query trực tiếp qua `db`. Nâng tổng số quan hệ FK đã xác nhận lỗi lên **5**: `award_participations`, `supplier_quotes`, `supplier_transactions`, `supplier_contacts`, `event_costs`.
- **Backlog Wave 1 (theo đúng hướng dẫn Codex, KHÔNG chỉ vá triệu chứng):** inventory FK toàn schema (đã xác nhận ảnh hưởng ít nhất 5 quan hệ ở trên, khả năng còn nhiều bảng khác chưa kiểm) để xác định constraint nào thực sự tồn tại trên MySQL vs SQLite, root-cause vì sao `ON DELETE CASCADE` không hoạt động, rồi sửa tận gốc ở tầng DB/migration — **không phải thêm `if` kiểm tồn tại thủ công ở từng route** (route-level check chỉ che 1 route, không đồng bộ hành vi cho các quan hệ FK khác).

### F16 — `total_cost`/`keynoteMonth`/chart tháng SUM() trả string trên MySQL, tái diễn cùng root cause F14 · **High / B / browser-production (sẽ chặn production tương lai) — ĐÃ FIX** (G1A.3 batch events-dashboard, phát hiện qua characterization R087/R098; sửa ngay theo đúng tiền lệ F14, không hỏi lại lần thứ 3 vì cùng root cause đã được owner duyệt fix 2 lần trước)
Cùng nguyên nhân F14 (mysql2 trả kết quả `SUM()` dạng string/DECIMAL, `COUNT()` trả number an toàn), nhưng ở 2 route khác chưa từng sửa khi F14 được vá (F14 chỉ sửa `GET /api/reports`):
- `GET /api/events` (`routes.js:1107`, R087): `total_cost` từ correlated subquery `SELECT COALESCE(SUM(amount),0) FROM event_costs ec WHERE ec.event_id=e.id` — string trên MySQL kể cả khi giá trị là `0` (MySQL suy ra kiểu trả về của `COALESCE(SUM(...), 0)` theo kiểu rộng nhất trong các nhánh, tức `DECIMAL`, dù nhánh thực tế chạy là literal `0`).
- `GET /api/dashboard` (`routes.js:1226-1227`, R098): `overview.events.keynoteMonth` từ `SUM(misa_keynotes)` — cùng lỗi.
- `GET /api/dashboard` `charts.assocActivityMonthly` (`routes.js:1242-1248`): mảng 12 tháng từ `SUM(c) c FROM (UNION ALL 2 subquery COUNT())` — **lỗi tinh vi hơn F14/keynoteMonth: chỉ lộ ra khi GROUP BY thực sự sinh ≥1 dòng khớp điều kiện** (có dữ liệu tương tác/tài trợ hiệp hội trong tháng/năm hiện tại); khi không có dữ liệu khớp, câu SQL trả 0 dòng hoàn toàn (không phải dòng có SUM=NULL) nên `monthly()` giữ nguyên giá trị mặc định `Array(12).fill(0)` — number thật — che giấu bug hoàn toàn trên môi trường test/dev thường trống dữ liệu. Phát hiện bằng cách chủ động seed 1 tương tác hiệp hội đúng ngày hôm nay rồi kiểm tra type của đúng ô tháng đó (không dựa vào giá trị mặc định 0).
- Sửa tận gốc tại 3 helper dùng chung trong `GET /api/dashboard` (`one()`, `grouped()`, `monthly()`) bằng `Number(...)` bọc quanh kết quả `.c` — không chỉ sửa 2 chỗ đang lỗi mà củng cố mọi field số hiện tại VÀ tương lai của dashboard (kể cả các field COUNT()-based hiện đang an toàn, phòng khi sau này đổi sang SUM()). `total_cost` ở `GET /api/events` sửa riêng tại điểm map `rows`.
- Test mới xác nhận `typeof === 'number'` cho toàn bộ field số trong `overview`, toàn bộ phần tử `charts.*Monthly` (kể cả ép seed dữ liệu để lộ đúng nhánh SUM), và `charts.reportersByBeat/assocByField[].value`.

### F17 — `POST /api/monitor/scan` trả 500 trên MỌI lượt quét thật trên MySQL do cột `sources.mode` không tồn tại · **P1 High / A / production (endpoint quét tin hoàn toàn hỏng trên MySQL) — ĐÃ FIX** (G1A.3 batch monitor phần 1, phát hiện qua characterization R110)
`server/db.js` `migrate()` thêm cột `sources.mode` bằng `ALTER TABLE sources ADD COLUMN mode TEXT NOT NULL DEFAULT 'rss'`. MySQL **không cho phép cột `TEXT`/`BLOB`/`GEOMETRY`/`JSON` có `DEFAULT` literal** (lỗi `"BLOB, TEXT, GEOMETRY or JSON column can't have a default value"`), nên câu ALTER này **luôn fail trên MySQL** — nhưng `migrate()` dùng helper `add(sql) { try { db.exec(sql); } catch {} }` **nuốt hết mọi lỗi im lặng**, nên thất bại này không bao giờ lộ ra qua log hay exception khởi động. Kết quả: cột `mode` **không tồn tại** trên schema MySQL thực tế, dù code CREATE TABLE và mọi route đều giả định nó có.
- Hậu quả thực tế: `monitor.js runScan()` bước 2 (fetch RSS không điều kiện theo `queryIds`, chạy trên MỌI lượt gọi `POST /api/monitor/scan`) query `WHERE enabled=1 AND type='news' AND (mode IS NULL OR mode='rss')` → MySQL báo `Unknown column 'mode' in 'where clause'` → route catch → **500 cho mọi lượt quét thật trên MySQL**, không chỉ characterization test. Đây là bug production nghiêm trọng hơn F14/F16 (không phải sai lệch số liệu mà là **hỏng hoàn toàn 1 chức năng chính** — quét giám sát truyền thông).
- Phát hiện bằng test R110 (network-safe: tắt hết `sources` + `scan_queries` include rỗng để không gọi mạng thật khi characterize) — 500 lộ ra ngay ở lần chạy MySQL đầu tiên, không cần dữ liệu RSS thật.
- **Sửa tận gốc** (không phải vá triệu chứng), 2 phần:
  1. Đổi kiểu cột từ `TEXT` sang `VARCHAR(20)` (`ALTER TABLE sources ADD COLUMN mode VARCHAR(20) NOT NULL DEFAULT 'rss'`) — hợp lệ trên cả MySQL và SQLite (SQLite dùng type affinity, không phân biệt `VARCHAR`/`TEXT`).
  2. Sửa helper `add()` trong `migrate()`: chỉ nuốt lỗi "duplicate column"/"already exists" (idempotent bình thường), còn mọi lỗi khác phải `console.error` ra — để lớp lỗi tương tự (cú pháp DDL không tương thích MySQL) không còn âm thầm biến mất trong tương lai, phòng ngừa tái diễn class bug này ở các cột khác.
- Đã kiểm tra toàn bộ các dòng `add("ALTER TABLE ...")` khác trong `migrate()`: chỉ có đúng 1 dòng dùng pattern `TEXT ... DEFAULT` (dòng vừa sửa), không phát hiện thêm trường hợp tương tự.
- Xác nhận sau fix: 47/47 test batch monitor phần 1 xanh trên cả SQLite và MySQL; toàn bộ regression suite (481 integration test) không hồi quy.
- **Remediation theo ChatGPT audit (commit `e684918`, Finding D/E "Must do"):** helper `add()` trong `migrate()` ban đầu chỉ `console.error()` lỗi lạ rồi vẫn tiếp tục boot — audit chỉ ra đây **vẫn cùng class failure đã gây F17**, chỉ khác là nay có log thay vì im lặng hoàn toàn (app vẫn có thể chạy với schema thiếu). Đã sửa: mọi lỗi không phải `duplicate column`/`already exists` giờ **rethrow**, làm app dừng khởi động ngay thay vì boot với schema chưa hoàn chỉnh. Tách logic phân loại ra hàm thuần `isIgnorableMigrationError()` (export từ `db.js`) để test được mà không cần re-run `migrate()` thật. Đồng thời thêm `mode VARCHAR(20) NOT NULL DEFAULT 'rss'` trực tiếp vào canonical `CREATE TABLE sources` (Finding E, khuyến nghị) — fresh schema không còn phụ thuộc migration mới có field này, giữ nguyên `ALTER TABLE` cho DB cũ. Test mới `BR-VAL-041/042` (`server/test/unit-validation-formatter.test.js`) xác nhận cả 2 nhánh: lỗi duplicate được tolerate, lỗi lạ được propagate.

### F18 — SUM()/AVG() trả string trên MySQL, tái diễn cùng root cause F14/F16 ở 3 route report chưa từng sửa · **P1 High / B / báo cáo tài chính hiển thị sai số trên MySQL — ĐÃ FIX** (G1A.3, phát hiện lúc rà soát toàn bộ SUM()/AVG() trong routes.js trước khi tuyên bố route mapping 145/145; không thuộc batch "ai" đang làm — tự phát hiện khi kiểm tra tính đầy đủ của G1A.3 trước khi đóng gate)
Cùng nguyên nhân F14/F16 (mysql2 trả kết quả `SUM()`/`AVG()` dạng string/DECIMAL, `COUNT()` trả number an toàn), lan rộng hơn phạm vi đã sửa trước đây — F14 chỉ sửa 4 tổng đơn của `GET /reports` (`spend.total`, `events.total`, `fees.total`, `grandTotal`), F16 chỉ sửa `GET /events`/`GET /dashboard`. 3 route/nhóm field khác **chưa từng được chạm tới**:
1. `GET /reports` (R052, `routes.js`): **9 mảng breakdown** dùng để vẽ chart (`spend.byMonth/byOrg/byPerson/byType/byStaff`, `fulfillment`, `events.byCategory/byEvent`, `fees.byOrg`) — mỗi phần tử `.amount` vẫn là string trên MySQL dù các tổng đơn cùng route đã được F14 sửa. Phát hiện thêm: `tiers.t1-t4` (`SUM(CASE WHEN...THEN 1 ELSE 0 END)`) cũng trả string trên MySQL dù đang đếm số nguyên, không riêng `SUM(amount)` mới bị.
2. `GET /reports/by-staff` (R053): `spend` (từ `SELECT COALESCE(SUM(amount),0) s...`) chưa từng được `Number()` wrap — route này chưa ai chạm tới kể từ F14. `avgScore` (`ROUND(AVG(p.relationship_score))`) cũng cần `Number()` khi có giá trị.
3. `GET /reports/by-unit` (R054): `spend` từ subquery trực tiếp trong `SELECT`, chưa `Number()`.
4. `GET /reports/awards` (R055): `mediaCost` (`SELECT COALESCE(SUM(amount),0) s...`) chưa `Number()`, khiến `awardCostOf()` cộng `cost + partBudget + mediaCost` **NỐI CHUỖI** khi `mediaCost` là string trên MySQL (ví dụ xác nhận trực tiếp: `totalCost="0300000"` thay vì `300000`) — sai số liệu báo cáo chi phí giải thưởng, mức độ nghiêm trọng cao vì đây là báo cáo tài chính.
- Xác nhận trực tiếp cả 4 điểm bằng script gọi route thật trên MySQL test DB trước khi sửa (không suy đoán).
- **Sửa tận gốc:** thêm helper `numField(rows, key='amount')` dùng chung cho `GET /reports` (bọc `Number()` cho field `amount` trên mọi dòng của 9 mảng breakdown một lượt, tương tự cách tiếp cận helper-level đã được ChatGPT audit khen ở F16); `tiers` build lại thành object mới với 4 field `Number()`-wrap. `GET /reports/by-staff`: `spend`/`avgScore` wrap tại điểm build response, `avgScore` giữ `null` khi AVG() rỗng (không ép về 0 — đây chính là hành vi đã ghi ở **BR-CALC-009**, nay có test characterize đúng). `GET /reports/by-unit`: wrap `r.spend` trong `rows.forEach()`. `GET /reports/awards`: wrap `mediaCost` ngay tại điểm lấy từ DB, trước khi truyền vào `awardCostOf()` — sửa cả field trả về lẫn phép cộng `totalCost` cùng lúc.
- Sửa ngay không hỏi lại (cùng tiền lệ F13/F14/F16/F17 — cùng root cause đã owner duyệt fix nhiều lần, và đây là bug hiển thị sai số liệu tài chính, không phải trade-off thiết kế cần quyết định).
- 10 test mới trong `integration-reports.test.js` xác nhận toàn bộ field number đúng cả 2 driver, bao gồm test cộng số thực tế (không chỉ kiểm `typeof`) để bắt lỗi nối chuỗi nếu tái diễn.
- Đã rà soát toàn bộ `SUM(`/`AVG(` trong `routes.js` và các file khác (`monitor.js` xác nhận trước đó chỉ dùng `COUNT()`, an toàn) — không còn điểm nào thiếu `Number()` sau fix này.

### F19 — `POST /api/ai/interaction-voice` trả 502 trên MỌI lượt trích xuất có tên người/tổ chức trên MySQL do `COLLATE NOCASE` không tồn tại · **P1 High / A / production — ĐÃ FIX (Codex, 2026-08-27)**
Phát hiện qua `server/test/integration-ai-golden.test.js` (AI-E001 golden happy, chạy trên MySQL với Gemini/fetch mock — genJSON trả `person_name`/`org_name` hợp lệ, không hề động tới mạng thật). Route 502 với message trực tiếp từ MySQL: `Unknown collation: 'NOCASE'`.
- Root cause: `server/ai.js:26` (`matchPerson`) và `server/ai.js:30` (`matchOrg`) dùng `... LIKE ? COLLATE NOCASE ...` — cú pháp collation của SQLite, MySQL không có collation tên `NOCASE`. Hai hàm này CHỈ được gọi từ route `/ai/interaction-voice` (đã grep xác nhận, không dùng ở route nào khác) — mỗi khi Gemini trả về `person_name`/`org_name` khác rỗng (gần như luôn luôn theo đúng mục đích route), route throw ngay khi tra cứu khớp người/tổ chức, dù phần trích xuất AI đã thành công.
- Đây là bug production thật trên MySQL (driver mặc định `DB_CLIENT=mysql`), không phải hành vi cần characterize — SQLite (driver dev/test mặc định trước đây) không phát hiện được vì `NOCASE` hợp lệ ở đó, đúng lý do vì sao chưa ai bắt được cho tới khi có test chạy dual-driver với Gemini mock.
- **Phương án đã chọn:** bỏ `COLLATE NOCASE` khỏi 2 query thay vì thêm rule dịch chung; không đổi hành vi cần thiết của lookup tên trên hai driver.
- **Resolution:** bỏ `COLLATE NOCASE` khỏi cả hai query; giữ semantics `LIKE` hiện hành trên SQLite/MySQL. Regression `AI-E001` xanh trên cả hai driver.
- **Trạng thái:** CLOSED; thay đổi code chỉ ở `server/ai.js:26,30`, có thể rollback độc lập.

### F20 — `groundIngest()`/`siteGroundIngest()` báo `added>0` nhưng dòng `mentions` không thấy khi SELECT lại ngay sau, CHỈ trên MySQL · **P1 High / A — ĐÃ FIX (Codex, 2026-08-27)**
Phát hiện qua `integration-ai-golden.test.js` AI-E008/AI-E009 (golden set, network-safe bằng fetch mock, không liên quan tới Gemini/mạng thật) — CHỈ fail trên MySQL, PASS ổn định trên SQLite (18/18).
- Triệu chứng quan sát được (đã debug trực tiếp, không suy đoán): gọi `monitor.groundIngest(q)` (hoặc `runScan()` với nguồn `mode='site'`) trên MySQL test DB — kết quả trả về `{fetched:1, added:1}` (báo thành công), nhưng `db.prepare('SELECT * FROM mentions WHERE query_id=?').get(q.id)` NGAY SAU ĐÓ không tìm thấy dòng vừa lưu; `SELECT * FROM mentions` (không filter) chỉ thấy 1 dòng cũ có `id=1, query_id=NULL, title=NULL` (nghi là dòng seed/placeholder có sẵn, KHÔNG liên quan) — dòng mới KHÔNG xuất hiện dù `insMention.run(...).changes` báo `>0`.
- Đã loại trừ: không phải lỗi cú pháp SQL bị nuốt và không phải FK. Phân tích ban đầu nghi worker race đã được kiểm tra lại và **DISPROVED**: lỗi nằm ở named binding trước khi worker thực thi SQL.
- **Trạng thái:** CLOSED; `AI-E008`/`AI-E009` xanh trên MySQL sau khi sửa adapter. Giả thuyết worker race ban đầu đã được loại trừ bằng thí nghiệm trực tiếp.
- **Root cause đã xác định:** không phải race/async trong `mysql-worker.js`. `MySQLSyncDatabase.prepare(...).run(object)` truyền object named binding thẳng vào `mysql2.query()`. Placeholder SQLite `@name` bị MySQL hiểu là session user variable và trở thành `NULL`; do đó `affectedRows=1` nhưng `query_id`/các field named bị ghi sai.
- **Resolution:** thêm `bindSqliteNamedParams()` tại `server/mysql-sync.js`, chuyển `@name` ngoài literal/comment thành `?` và bind theo đúng thứ tự; giữ nguyên positional path. Regression unit kiểm tra lặp tên/literal/comment/thiếu binding; `AI-E008` và `AI-E009` đã xanh MySQL.
- Đây là fix ở tầng adapter nên bao phủ mọi caller dùng SQLite-style object binding, không vá riêng `saveMention()`. Giả thuyết worker race ban đầu được đánh dấu DISPROVED bằng thí nghiệm trực tiếp.
- **Trạng thái:** CLOSED; thay đổi có thể rollback độc lập.

### F21 — `GET /api/files/:id` phục vụ file `private` của MỌI owner_type khác `person` (award/supplier/event/agreement/work_log) không gate gì · **P0 Critical / bypass D13.3, IDOR — ĐÃ FIX (Codex audit, remediation Claude 2026-08-31)**
Phát hiện qua audit Codex trên Evidence Bundle gộp `memory-bank/21-audit-bundle-f15-rbac-exp-b1-b6.md` (F15 → RBAC-EXP-B6): tái hiện HTTP thật trên SQLite tạm — user `viewer` tải attachment `award_doc` có `audience_visibility='private'` mặc định nhận **200 và toàn bộ nội dung file**.
- Root cause: `GET /api/files/:id` (`server/routes.js`) chỉ gate khi `att.kind==='id_doc'` (luôn mật) hoặc `att.owner_type==='person'` (dùng `policy.canReadAttachment()`); mọi `owner_type` khác (award/supplier/event/agreement/work_log — 5 owner_type còn lại có route upload file thật) rơi thẳng xuống `res.sendFile()` không qua bất kỳ policy check nào — hành vi này tồn tại từ khi route được viết, không phải regression của batch RBAC-EXP nào; chỉ bị phát hiện khi audit rà tới hotspot file/download.
- **Resolution (đúng gốc rễ, dùng chung 1 policy path thay vì vá từng route):**
  1. `server/policy-engine.js#canReadAttachment()` mở rộng nhận thêm `entity`/`record`/`parentOwnerId` — thêm 2 nhánh owner-bypass giống hệt `canReadField()`: executor sở hữu bản ghi Direct (`ownerValue(entity,record)===principal.id`) hoặc Inherited (`parentOwnerId===principal.id`) thì được xem private; Global (`supplier`) và các entity chưa map KHÔNG có owner-bypass — chỉ Admin/Super Admin xem được private, đúng pattern đã có sẵn cho field Confidential trên Global entity (`service_fee_pct`/`deposit_pct`).
  2. `server/routes.js`: thêm `ATTACHMENT_OWNER_ENTITY` — map cố định `owner_type -> {entity, module, table}` cho đúng 6 owner_type có thật trong hệ thống (person/award/supplier/event/agreement/work_log). `GET /files/:id` tra map trước tiên: **owner_type không có trong map → 403 fail-closed ngay, kể cả Admin/Super Admin** (không suy diễn hành vi cho owner_type lạ/chưa từng khai). owner_type hợp lệ: kiểm `rbac.can(role, module, 'view')` (defense-in-depth, hiện tại 4 vai trò D13 đều có `view` trên 4 module liên quan) rồi fetch bản ghi cha thật (`SELECT * FROM <table> WHERE id=owner_id`) truyền vào `canReadAttachment()` để xét owner/visibility đúng entity đó.
  3. `person` giữ nguyên hành vi cũ 100% (Global, không owner bypass, chỉ check `audience_visibility==='public'`) — xác nhận bằng test cũ D13-011 vẫn xanh không sửa.
- Test mới: `integration-awards.test.js` D13-078 (award, Direct — viewer/executor-không-sở-hữu 403, owner-executor/admin 200), D13-079 (owner_type lạ fail-closed 403 cho MỌI role kể cả Admin); `integration-suppliers.test.js` D13-080 (supplier, Global — private luôn Admin-only, kể cả executor tự upload cũng 403).
- Phạm vi có chủ đích KHÔNG mở rộng (báo cáo, không tự sửa — theo `12-phase-maintenance.md` mục 4): `DELETE /attachments/:aid` đã fail-closed 403 sẵn cho owner_type≠person (an toàn, chỉ là hạn chế chức năng, không phải lỗ hổng); upload file cho award/supplier/event/agreement/work_log vẫn dùng `requirePerm(module,'edit')` thô theo role, chưa gate theo owner_id của Direct entity (bất kỳ executor nào cũng upload được vào award/event của người khác) — ghi nhận backlog P2, không phải P0/P1 nên không giữ gate theo `CLAUDE.md` mục 5; metadata attachment (`id/original_name/mime`) trong `GET /awards/:id`/`GET /suppliers/:id`/... vẫn trả không lọc theo owner (chỉ lộ tên file, không lộ nội dung) — backlog P3.
- **Codex ACCEPTED WITH BACKLOG (2026-08-31, re-audit commit `051a9f9`):** xác nhận độc lập `GET /files/:id` nay map owner type tĩnh, kiểm module-view + ownership/visibility, unknown type trả 403 fail-closed (`routes.js:685`, `policy-engine.js:65`). Focused SQLite 101/101 + focused MySQL 101/101 pass; full security/SQLite/MySQL/mapping/G0 verifier/diff-check đều exit 0; F15 xác nhận lại vẫn đúng (24 FK thật trên MySQL tạm). 2 backlog không chặn, gắn nhãn W1.FILE (roadmap có sẵn) — **P2:** upload file của Direct entity vẫn chỉ gate theo role, chưa kiểm owner. **P3:** metadata file trong detail response chưa lọc theo visibility.
- **P3 xác định là WON'T-FIX (2026-08-31, batch W1.POLICY.2):** đã xem xét lại — metadata attachment (`id/original_name/mime`) là "sự tồn tại" của file, không phải "nội dung" của nó (nội dung mới cần tải qua `GET /files/:id`, đã gate đủ ở fix P0 trên). Theo đúng nguyên tắc D13 "existence vs content" (`02-decisions.md` dòng 96: mọi role ≥ Viewer thấy sự tồn tại bản ghi + field public; owner/Admin/Super Admin mới thấy đầy đủ NỘI DUNG), việc mọi role thấy được tên file trong khi chỉ owner/Admin/Super Admin tải được nội dung file là **đúng thiết kế D13, không phải lỗ hổng bỏ sót** — không sửa code, đóng backlog này ở đây (không chuyển sang batch nào khác).
- **P2 ĐÃ FIX (2026-08-31, batch `W1.FILE-P2`):** `POST /awards/:id/files`, `POST /events/:id/files`, `POST /agreements/:id/files`, `POST /work-logs/:id/files` (award/event/agreement/work_log là entity Direct) nay fetch record thật + gọi `policyService.assertWritable({principal, entity, action:'edit', record})` trước khi insert attachment — giống hệt pattern PUT của chính 4 entity này, thay `requirePerm(module,'edit')` thô theo role. Test: `integration-awards.test.js` D13-089, `integration-events-dashboard.test.js` D13-086, `integration-partners.test.js` D13-087/D13-088 (executor upload vào record người khác trả 403, vào record chính mình trả 200, admin luôn 200). **Ngoài phạm vi có chủ đích:** `POST /suppliers/:id/files` KHÔNG sửa — supplier là entity Global (executor sửa được bất kể ai tạo, không có owner-bypass trên PUT/DELETE của chính nó), nên gate thô theo role hiện tại là đúng thiết kế, không phải P2. Xem batch contract `18-g1b-rbac-batch-contract.md#batch-w1file-p2-2026-08-31`. **Không còn backlog nào của F21/W1.FILE.**
- **Trạng thái:** CLOSED; regression đầy đủ xanh cả 2 driver sau fix (xem `15-changelog.md`).

### F22 — RBAC-CUTOVER không migrate user role legacy `pr_staff` sang `executor` trong DB thật · **P1 High / lockout toàn bộ user cũ sau deploy — ĐÃ FIX (Codex audit, remediation Claude 2026-08-31)**
Phát hiện qua cùng audit Codex ở trên. `02-decisions.md` (amendment §G.1, owner chốt 2026-08-30 cùng batch RBAC-CUTOVER) yêu cầu ánh xạ cố định `pr_staff`→`executor` cho MỌI user hiện có, nhưng batch RBAC-CUTOVER (`f170e62`) chỉ rename khoá MATRIX trong code (`rbac.js`), KHÔNG có migration cập nhật dữ liệu `users.role` đã tồn tại trong DB — Codex thí nghiệm trực tiếp: đặt 1 user thành `pr_staff`, khởi động app lại, role vẫn giữ nguyên `pr_staff` (không tự chuyển).
- Risk xác nhận: `rbac.MATRIX` từ sau RBAC-CUTOVER không còn khoá `pr_staff` → `rbac.can('pr_staff', module, action)` luôn trả `false` cho MỌI module/action → user cũ vẫn đăng nhập được (session/password không đổi) nhưng bị 403 ở toàn bộ route còn dùng `requirePerm`, và bị fail-closed ở mọi route đã gắn PolicyEngine (`canWrite`/`canReadField` đều kiểm `principal.role !== 'executor'` literal) — lockout hoàn toàn ngay sau deploy, không phải lý thuyết.
- **Resolution:** `server/db.js#migrate()` thêm `db.exec("UPDATE users SET role='executor' WHERE role='pr_staff'")` — chạy mỗi lần app khởi động (đúng vị trí `migrate()` đã chạy sẵn cho mọi ALTER TABLE khác), tự nhiên idempotent (chạy lại ảnh hưởng 0 dòng nếu không còn user nào mang role đó), không cần bọc qua `add()`/`isIgnorableMigrationError()` (không phải DDL, không có lỗi "duplicate column" cần tolerate). `migrate` export thêm từ `db.js` (giống tiền lệ export `isIgnorableMigrationError` ở F17) để test gọi trực tiếp mà không cần restart app thật.
- Test mới: `integration-db-contract.test.js` DB-CONTRACT-006 — insert user `role='pr_staff'` trực tiếp, gọi `migrate()`, xác nhận role đã chuyển `executor`; gọi `migrate()` lần 2 xác nhận idempotent (không lỗi, không đổi thêm).
- **Codex ACCEPTED WITH BACKLOG (2026-08-31, re-audit commit `051a9f9`):** tái hiện restart thật — user bắt đầu `pr_staff`, sau boot thành `executor` và có lại `partners:view` (`db.js:709`). Cùng đợt verify độc lập với F21 (focused 101/101 cả 2 driver, full suite/mapping/verifier/diff-check exit 0).
- **Trạng thái:** CLOSED; UPDATE tự nhiên an toàn chạy lặp, rollback = revert đúng dòng thêm trong `migrate()`.

### F23 — 3 route "tạo nhanh nhắc lịch" ghi `important_dates` nhưng gate theo quyền `view` của module cha thay vì `reminders:create` thật · **P2 Medium / permission-boundary mismatch — ĐÃ FIX (tự phát hiện qua audit có hệ thống, batch `W1.POLICY.2 write-side` 2026-08-31)**
Phát hiện qua audit có hệ thống toàn bộ route ghi (POST/PUT/DELETE) trong `server/routes.js`, đối chiếu module khai ở `requirePerm()` với bảng thực sự bị ghi — nhằm đóng phần WRITE của roadmap item `W1.POLICY.2` ("CI static rule cấm raw SQL ghi field ngoài policy"). `POST /partners/:id/fees/:fid/remind` (R028), `POST /awards/:id/remind` (R071), `POST /events/:id/remind` (R096) đều `INSERT INTO important_dates` nhưng đăng ký với `requirePerm('partners'|'awards'|'events', 'view')` — quyền ĐỌC của module cha — thay vì `requirePerm('reminders', 'create')` như route chính thống `POST /reminders` (R040) đã dùng đúng từ đầu.
- Risk xác nhận qua MATRIX (`server/rbac.js`): vai trò `viewer` có `reminders: ['view']` (KHÔNG có `create`) nhưng có `partners/awards/events: ['view']` — nghĩa là `viewer` (vai trò chỉ-xem, theo D13.1 không được sửa/tạo dữ liệu nghiệp vụ) vẫn tạo được bản ghi `important_dates` thật qua 3 lối tắt này, bỏ qua đúng ranh giới quyền `reminders:create` mà mọi route khác của cùng bảng đều tôn trọng. Không phải IDOR/lộ dữ liệu mật (severity thấp hơn F21) nhưng là 1 permission-boundary mismatch thật, đúng loại lỗi mà roadmap W1.POLICY.2 muốn CI rule chặn.
- **Resolution:** đổi cả 3 route sang `requirePerm('reminders', 'create')`. Vì `executor`/`admin`/`super_admin` đều đã có sẵn `reminders:create` trong MATRIX, thay đổi này KHÔNG ảnh hưởng hành vi của 3 role đó — chỉ chặn đúng `viewer`.
- CI guard mới: `scripts/verify-g0.mjs#verifyImportantDatesGate()` — quét mọi route trong `routes.js`, route nào có thân hàm chứa `INSERT INTO important_dates` phải đăng ký `requirePerm('reminders', *)`, nếu không FAIL rõ ràng route/module vi phạm. Phạm vi hẹp có chủ đích (chỉ 1 bảng `important_dates`, không phải bộ máy tổng quát map bảng→module) — đã thử nghiệm bằng cách revert tạm 1 route để xác nhận full verifier bắt được lỗi trước khi coi là đủ.
- Test mới: `integration-partners.test.js` D13-090, `integration-awards.test.js` D13-091, `integration-events-dashboard.test.js` D13-092 (viewer trả 403, executor vẫn 200).
- **Quyết định về phần còn lại của W1.POLICY.2 (wrapper `authorizedInsert/Update` tổng quát):** một audit độc lập (agent) quét TOÀN BỘ route ghi trong `routes.js` xác nhận không còn bất kỳ write-bypass nào khác (mọi `buildInsert`/`buildUpdate`/`db.prepare(...).run()` ghi đều có `assertWritable`/`prepareCreate`/`prepareUpdate`/`moduleAdminOnlyGate`/`requirePerm` đúng đứng trước) — 10 nhóm write-bypass gốc của F1 đã đóng hết qua các batch RBAC-EXP-B1..B6 trước đó. Vì không còn lỗ hổng thật nào cần một wrapper bắt buộc mới đóng, và routes.js hiện đã có choke point thực chất (`assertWritable`/`prepareCreate`/`prepareUpdate`) dùng nhất quán ở mọi route Direct/Global/Module-admin-only, xây thêm 1 lớp wrapper `authorizedInsert/Update` thuần kiến trúc (không có bug cụ thể nào thúc đẩy) sẽ là premature abstraction — không làm, ghi nhận quyết định này rõ ràng thay vì âm thầm bỏ qua. Xem `04-ROADMAP.md` hàng `W1.POLICY.2`.

### F24 — 4 route upload file (award/event/agreement/work_log) chạy authorization SAU khi Multer đã ghi file thật vào đĩa · **P1 High / deny có side-effect, file mồ côi — ĐÃ FIX (Codex audit trên bundle đóng W1, remediation Claude 2026-08-31)**
Phát hiện qua audit Codex trên Evidence Bundle gộp `memory-bank/22-audit-bundle-w1-close.md` (W1.POLICY.2/W1.FILE-P2/W1.ADMIN) — Codex tái hiện HTTP thật trên SQLite tạm: `executor` upload vào `award` của `admin` nhận đúng `403`, nhưng số file trong `UPLOAD_DIR` vẫn tăng `0 → 1` (Multer đã ghi file vật lý trước khi handler kịp gọi `assertWritable()`).
- Root cause: `POST /agreements/:id/files`, `POST /work-logs/:id/files`, `POST /awards/:id/files`, `POST /events/:id/files` đăng ký `upload.array(...)` (Multer, disk storage — `server/uploads.js:8`) đứng TRƯỚC handler; batch `W1.FILE-P2` (F21 §P2 fix) đặt `assertWritable()` bên TRONG handler — đúng thứ tự middleware Express nghĩa là Multer đã ghi file vào `UPLOAD_DIR` xong mới tới lượt authorization chạy. Request bị `403` vẫn để lại file mồ côi trên đĩa, không có row `attachments` quản lý/dọn dẹp — vi phạm nguyên tắc "deny phải không có side-effect"; lặp lại nhiều lần có thể làm đầy storage.
- **Resolution (đúng gốc rễ — authorize trước Multer, không phải dọn file sau khi đã ghi):** middleware mới `requireFileWrite(entity, table, moduleLabel)` (`server/routes.js`) fetch record thật + gọi `policyService.assertWritable({principal, entity, action:'edit', record})` TRƯỚC `upload.array(...)`, trả 403 và KHÔNG gọi `next()` nếu không hợp lệ — Multer chỉ chạy (và chỉ ghi file) sau khi authorization đã pass. Đặt `requireFileWrite(...)` làm middleware đầu tiên cho cả 4 route; handler phía sau (đã pass check) không cần kiểm tra lại — xoá logic `assertWritable` trùng lặp trong `govFileUpload()`/2 handler inline award+event. Giữ nguyên message lỗi 403 gốc của từng route (`moduleLabel` tham số hoá: `'partners'` cho agreement/work_log — đúng generic message cũ của `govFileUpload`, `'awards'`/`'events'` cho 2 route inline) — không đổi hành vi ngoài việc thêm side-effect-safety.
- Test mới: `server/test/integration-file-write-authz.test.js` (8 test table-driven, cả 2 driver) — cho mỗi entity (award/event/agreement/work_log): case deny (executor upload vào record người khác → 403 + row `attachments` KHÔNG tăng + số file trong `UPLOAD_DIR` KHÔNG tăng) và case đối chứng (executor upload vào record chính mình → 200 + CẢ 2 số liệu tăng đúng 1, xác nhận phép đếm thật sự nhạy chứ không phải assertion luôn pass). Đã thí nghiệm revert tạm fix (đưa `assertWritable()` trở lại sau `upload.array()`) để xác nhận test bắt đúng lỗi F24 (403 vẫn đúng nhưng file-count tăng `0→1`) trước khi coi test là đủ.
- **Trạng thái:** ĐÃ FIX, chờ Codex re-audit tập trung đúng finding này (theo `17-fast-track-collaboration.md` §8 — không mở lại toàn batch). Full regression sau fix: security 6/6, SQLite 811 total/803 pass/8 skip (+8), MySQL 811 total/810 pass/1 skip (+8), mapping 148/148, `verify-g0.mjs` PASS, `verify-g0-selftest` 6/6, `git diff --check` sạch.

### F25 — `POST /ai/interaction-voice-confirm` claim proposal xong mới ghi interaction: lỗi giữa chừng làm mất lệnh người dùng · **P1 High / confirm "thành công nửa chừng" — ĐÃ FIX (Codex audit Evidence Bundle `24-audit-bundle-w3voice-securecommand.md`, remediation Claude 2026-08-31)**
Phát hiện qua audit Codex trên bundle W3.VOICE.SECURE-COMMAND — Codex tạo trigger tạm ép `INSERT INTO interactions` lỗi, tái hiện HTTP thật trên SQLite: request đầu `502`; proposal bị khoá vĩnh viễn ở `status='confirmed'` nhưng `result_interaction_id=null` (interaction chưa từng được tạo); retry cùng idempotency key trả `200`/`interactionId:null` — trông như thành công nhưng người dùng không còn lệnh nào được ghi nhận thật.
- Root cause: atomic claim (`UPDATE voice_proposals SET status='confirmed' ...`) và các bước ghi tiếp theo (INSERT interaction, audit log, cập nhật `result_interaction_id`, CAS điểm) là các câu lệnh SQL RỜI RẠC không bọc transaction — bundle gốc gọi đây là "atomic gate" nhưng gate đó chỉ chống double-click (dedupe), không bảo vệ toàn bộ chuỗi nghiệp vụ khỏi lỗi giữa chừng.
- **Resolution:** thêm `withTransaction(fn)` (`server/db.js`) — bọc `BEGIN`/`COMMIT`/`ROLLBACK` thô qua `db.exec()`. An toàn dùng được vì cả 2 driver (`DatabaseSync` cho SQLite, `MySQLSyncDatabase` cho MySQL — 1 connection duy nhất qua worker thread, gọi đồng bộ bằng `Atomics.wait`) không có `await` xen giữa trong 1 request handler nên không request nào khác chen được vào giữa transaction. Route confirm nay bọc claim + insert interaction + audit + cập nhật `result_interaction_id` + CAS điểm trong 1 `withTransaction()` — lỗi ở bất kỳ bước nào đều `ROLLBACK`, proposal quay về đúng `pending`, có thể retry thật (không phải "giả vờ thành công"). Điều kiện TTL cũng chuyển vào ngay câu `UPDATE` claim (`AND expires_at > datetime('now')`) thay vì chỉ dựa vào `isExpired()` kiểm tra JS trước đó, để hết hạn đúng lúc claim cũng được coi là 1 dạng xung đột claim xử lý atomic, không phải race riêng.
- Test mới: `server/test/integration-voice-secure-command.test.js` — "lỗi giữa chừng khi ghi interaction" dùng CHECK constraint (MySQL) / TRIGGER (SQLite) tạm thời ép `INSERT` lỗi có điều kiện (`partner_name='FORCE_FAIL_MARKER'`), xác nhận: response đầu `502`, KHÔNG có interaction mồ côi, proposal quay về `pending`; retry cùng idempotency key sau khi gỡ lỗi tạo đúng 1 interaction. Không dùng mock JS (mock không xác nhận được hành vi transaction/rollback thật ở tầng DB).

### F26 — CAS điểm quan hệ stale chỉ bỏ qua phần điểm, vẫn tạo interaction — trái D14.4 · **P1 High / partial-success ngầm — ĐÃ FIX (Codex audit Evidence Bundle `24-audit-bundle-w3voice-securecommand.md`, remediation Claude 2026-08-31)**
Hợp đồng nguồn D14.4 (`02-decisions.md` §E) yêu cầu khi optimistic-concurrency snapshot khác hiện tại thì "từ chối và yêu cầu chuẩn bị lại", nhưng code batch gốc khi CAS `relationship_score` thất bại (điểm đã bị đổi song song kể từ lúc propose) vẫn tạo interaction, chỉ trả `scoreApplied:false` — người dùng xác nhận 1 hành động gồm "ghi nhận tương tác + cập nhật quan hệ" nhưng hệ thống âm thầm chỉ làm một nửa, đúng hành vi partial-success mà D14.4 cấm.
- **Resolution:** CAS điểm thất bại do snapshot stale (khác với thiếu QUYỀN sửa điểm — nhánh `PolicyForbiddenError` giữ nguyên hành vi cũ, vẫn tạo interaction bỏ qua phần điểm, vì đó là thiếu quyền chứ không phải dữ liệu lệch thời điểm) nay `throw` bên trong `withTransaction()`, ROLLBACK TOÀN BỘ (kể cả interaction vừa insert), trả `409 PROPOSAL_STALE` yêu cầu tạo lại đề xuất — đúng nghĩa "từ chối, không tạo được cả 2 phần" theo D14.4.
- Test sửa: "confirm: relationship_score bị đổi song song (stale)" đổi kỳ vọng từ `200`/`scoreApplied:false` sang `409 PROPOSAL_STALE` + `countInteractions()` không đổi + điểm giữ nguyên giá trị ghi song song + proposal quay về `pending` (có thể tạo proposal mới).

> **Superseded by owner decision 2026-09-02:** `relationship_score` nay nhập tay và hoàn toàn ngoài
> Voice/Gemini. Vì Voice không còn đọc/ghi điểm, một sửa tay điểm giữa propose/confirm không phải
> stale condition; interaction vẫn được xác nhận và score tay được giữ nguyên. F26 là lịch sử đúng
> của cơ chế cũ, không còn là hành vi runtime.

### P2 — `idempotencyKey` không bắt buộc, ghi `null` khi thiếu · **P2 / gộp remediation cùng F25 theo quyết định Codex (không audit vòng riêng)**
`idempotencyKey` được destructure nhưng không validate, ghi thẳng `null` xuống `voice_proposals.idempotency_key` khi client không gửi — mất khả năng retry đáng tin cậy khi mobile/network timeout (2 request không có key coi như 2 lần confirm khác nhau, gate atomic chỉ dedupe được khi CÓ key trùng).
- **Resolution:** bắt buộc `idempotencyKey` là string không rỗng, tối đa 200 ký tự; thiếu/rỗng/quá dài trả `400 VALIDATION_FAILED` trước khi chạm DB.
- Test mới: "confirm: thiếu hoặc rỗng idempotencyKey bị từ chối 400" (2 case: thiếu hẳn trường, chuỗi toàn khoảng trắng) — xác nhận proposal không bị claim bởi request không hợp lệ.

**Trạng thái chung F25/F26/P2: ĐÃ FIX — Codex ACCEPTED (2026-08-31)**, xác nhận độc lập bằng cách chạy lại 12/12 test voice trên cả SQLite/MySQL, tự xác nhận failure-path DB thật rollback đúng (không còn proposal `confirmed` mồ côi), transaction bọc đúng claim→interaction→audit→result pointer→CAS (`server/ai.js`), `PROPOSAL_STALE` không tạo interaction, `idempotencyKey` bắt buộc + validate, `verify-g0`/route mapping/`git diff --check` đều xanh. Full regression sau fix: security 6/6, SQLite 823 total/815 pass/8 skip (+2 so với batch gốc), MySQL 823 total/822 pass/1 skip (+2), mapping 150/150, `verify-g0.mjs` PASS, `git diff --check` sạch. Xem `24-audit-bundle-w3voice-securecommand.md` mục "Remediation F25/F26/P2" cho evidence đầy đủ.

### F27 — Parent entity (person/organization) không được đọc lại/so snapshot trước khi ghi interaction (TOCTOU) · **P1 High / interaction có thể trỏ tới bản ghi đã mất — ĐÃ FIX, Codex ACCEPTED cả 3 case (re-audit 2026-08-31)**
Phát hiện ngay trong vòng re-audit F25/F26/P2 — Codex ACCEPTED 3 fix trên nhưng phát hiện thêm P1 mới: proposal chỉ snapshot `id/name/relationship_score` (dùng cho CAS điểm), KHÔNG đọc lại person/org lúc confirm khi KHÔNG có score delta (nhánh CAS chỉ chạy khi `scoreDelta !== 0`). Codex tái hiện HTTP thật: (1) propose 1 interaction với person hợp lệ, không có score delta; (2) xoá person trước khi confirm; (3) confirm vẫn trả `200`, proposal thành `confirmed`, interaction mới giữ `partner_id` trỏ tới person đã bị xoá — trái D14.4 (server phải đọc lại bản ghi hiện tại và so snapshot trước khi ghi).
- Root cause: cơ chế optimistic-concurrency batch gốc chỉ bảo vệ đúng 1 trường (`relationship_score`) và chỉ chạy khi trường đó thực sự được sửa — không có cơ chế chung xác nhận toàn bộ parent đã chọn (person/org) còn tồn tại/còn đúng như lúc người dùng xác nhận.
- **Resolution:** đọc lại ĐÚNG các field đã snapshot (`name`/`org_name`/`relationship_score` cho person; `name`/`org_type` cho org) TRƯỚC khi ghi bất kỳ gì, cho MỌI parent đã chọn — không chỉ khi có score delta. Coi toàn bộ snapshot candidate (đã trả về cho người dùng lúc propose) là "revision" thực tế, thay vì thêm cột `revision` riêng + instrument mọi đường ghi `people`/`organizations` trong toàn bộ code base (phạm vi rộng hơn nhiều, rủi ro bỏ sót 1 đường ghi cao hơn so với so sánh trực tiếp bản ghi hiện tại). Khác với lỗi thật (ROLLBACK về `pending`, retry được), khi phát hiện stale (row bị xoá HOẶC bất kỳ field snapshot nào khác), proposal chuyển sang trạng thái **terminal `stale`** và COMMIT (không bao giờ insert interaction) — đúng đề xuất #3 của Codex, tránh proposal "sống lại" nếu dữ liệu vô tình quay về đúng snapshot cũ (vd điểm bị đổi rồi đổi lại).
- Test mới (`integration-voice-secure-command.test.js`, tăng 12→15): person bị xoá (không có score delta) → 409 `PROPOSAL_STALE`, không tạo interaction, proposal `stale`; organization bị xoá → tương tự; person đổi tên (KHÔNG phải điểm) → 409 (xác nhận drift-check không chỉ theo dõi điểm). Test stale-score cũ cập nhật: assert `status='stale'` (không còn `'pending'`) + retry cùng proposal vẫn 409.
- **Trạng thái:** ĐÃ FIX. Codex re-audit 2026-08-31 ACCEPTED cả 3 case bắt buộc (person bị xoá, organization bị xoá, revision/snapshot drift) và xác nhận độc lập 15/15 test pass trên cả 2 driver. F27 đóng. Xem `24-audit-bundle-w3voice-securecommand.md` mục "Remediation F27" cho evidence đầy đủ.

### F28 — Freshness re-check của F27 không khoá row trên MySQL nhiều instance (TOCTOU liên-process) · **P1 High / release blocker cho triển khai nhiều Cloud Run instance — CLOSED, Codex ACCEPTED (re-audit 2026-08-31)**
Phát hiện ngay trong chính vòng re-audit ACCEPTED F27: `fetchPersonSnapshot`/`fetchOrgSnapshot` (thêm cho F27) chỉ dùng plain `SELECT`, không giữ row lock. Trong 1 process, `withTransaction()` đảm bảo không có handler nào khác của CHÍNH process đó chen được vào giữa nên không sao — nhưng trên MySQL khi chạy nhiều Cloud Run instance (mỗi instance là 1 process/connection MySQL riêng), 1 instance khác vẫn có thể `UPDATE`/`DELETE` đúng row parent giữa lúc instance A đọc snapshot và lúc A `INSERT` interaction — race liên-process thật, không phải nội-process như F27.
- Root cause: F27 giả định đúng "trong 1 transaction, không gì chen được vào giữa" — đúng ở tầng ứng dụng/process nhưng không đủ ở tầng MySQL khi có nhiều connection độc lập ghi đồng thời vào cùng bảng.
- **Resolution:** thêm `isMysql` export từ `server/db.js` (dựa vào `DB_CLIENT` sẵn có); `fetchPersonSnapshot`/`fetchOrgSnapshot` (`server/ai.js`) dùng `SELECT ... FOR UPDATE` khi `isMysql`, giữ nguyên plain `SELECT` cho SQLite (claim `UPDATE` đã lấy write lock toàn DB, SQLite cũng không hỗ trợ cú pháp `FOR UPDATE`). Đúng đề xuất của Codex — không thêm cột `revision`, không mở rộng sang UI.
- Test mới (`integration-voice-secure-command.test.js`, tăng 15→17, MySQL-only qua `{ skip: !isMysql }`): 2 connection `mysql2/promise` độc lập với app — connA giữ `SELECT ... FOR UPDATE` (đúng câu SQL fix thêm), connB thử `UPDATE`/`DELETE` cùng row phải bị CHẶN cho tới khi connA COMMIT/ROLLBACK. Không đi qua HTTP route thật được vì `MySQLSyncDatabase` chặn đồng bộ main thread bằng `Atomics.wait` — nếu giữ lock trước rồi gọi HTTP trên cùng process sẽ tự deadlock chính test process (test đã giải thích rõ trong comment).
- **Trạng thái:** CLOSED (commit `d8ff14b`). Codex re-audit 2026-08-31 ACCEPTED — tự chạy lại độc lập SQLite 15/15, MySQL 17/17 (bao gồm 2 test dùng 2 connection độc lập chứng minh `FOR UPDATE` chặn `UPDATE` tới `COMMIT` và chặn `DELETE` tới `ROLLBACK`), `verify-g0`/route mapping/`git diff --check` đều pass. Quyết định của Codex: **`W3.VOICE.SECURE-COMMAND` backend đủ điều kiện CLOSED** — transaction rollback (F25), TTL atomic, idempotency (P2), stale terminal (F26), parent re-check (F27) và multi-instance lock (F28) đều đã có bằng chứng. Xem `24-audit-bundle-w3voice-securecommand.md` mục "Remediation F28" cho evidence đầy đủ.

### F29 — Form Vue dùng trực tiếp draft bất biến nên không nhận giá trị nhập · **P1 High / browser-production — FIXED (2026-09-01)**

- **Bằng chứng:** `toEventDraft()`/`toAwardDraft()`/`toEventCostDraft()`/`toInteractionCreateDraft()` và
  các draft People/Booking trả `Object.freeze(...)`, nhưng một số form đưa thẳng kết quả vào
  `reactive(...)`. Vue không proxy object non-extensible: thí nghiệm Node `isReactive(reactive(Object.freeze({ name: '' })))`
  trả `false`; gán `name` không đổi giá trị. Vì vậy người dùng có thể mở form nhưng không nhập được,
  sau đó bị validation từ chối — khớp triệu chứng "không thêm được".
- **Resolution:** mọi form bị ảnh hưởng sao chép draft (`reactive({ ...toXxxDraft(...) })`) trước khi
  bind `v-model`, vẫn giữ `Object.freeze` ở boundary domain/payload. Phạm vi gồm Event core/cost,
  Award core, Interaction create, People create, Booking create/edit trên Desktop và Native.
- **Regression evidence:** `UI-FORM-001` kiểm tra 14 composition: tái hiện trap bằng Vue thật, xác
  nhận copy là reactive/có thể gán và khóa source form vào pattern copy. Không làm client trở thành
  nơi phân quyền; payload vẫn qua allowlist và PolicyEngine server-side.

### F30 — Form create truyền `record=null` vào draft đọc thuộc tính trực tiếp · **P1 High / browser-production — FIXED (2026-09-01)**

- **Bằng chứng:** thí nghiệm thật `toEventDraft(null)` và `toAwardDraft(null)` đều ném
  `Cannot read properties of null (reading 'name')`. Feature create truyền `state.record=null`, nên
  màn hình có thể lỗi ngay khi bấm Thêm, độc lập với route hoặc quyền API.
- **Resolution:** normalize `record || {}` ngay tại boundary draft Event/Award/Award participation;
  mọi caller (Desktop/Native/create/edit) được an toàn mà không cần phân nhánh rải rác trong form.
- **Regression evidence:** `UI-EVENT-001`, `UI-AWARD-002`, `UI-AWARD-003` gọi trực tiếp draft với
  `null`; các UI contract tests và production build đều chạy sau bản vá.

### F31 — Đổi hash có thể giữ mini-app Vue cũ trên DOM · **P1 High / browser-production — FIXED (2026-09-02)**

- **Bằng chứng:** từng native strangler slice giữ route của mình trong một `ref` riêng. Resolver
  hash dùng chuỗi `||` và dừng ngay khi route mới khớp, nên những `ref` của màn hình trước đó nằm
  sau resolver vừa khớp không được xoá. Fake-native runtime tái hiện bằng cách chuyển liên tiếp
  `#events → #admin → #monitor → #reports → #interactions → #awards → #people → #person/1 →
  #partner/1 → #suppliers/1`: trước bản vá, có lúc nhiều `.mds-mobile-app` cùng tồn tại; nội dung
  và action của màn hình cũ có thể còn xuất hiện dù URL đã đổi.
- **Resolution:** `frontend/src/App.vue` tập hợp toàn bộ route ref vào một danh sách bất biến và
  `clearUiFeatureRoutes()` trước khi bất kỳ resolver nào claim hash mới. Thay đổi chỉ quản lý state
  presentation; API, principal và PolicyEngine server-side không thay đổi.
- **Regression evidence:** `UI-CHAR-005` khoá thứ tự reset bằng source contract. Runtime fake-native
  390×844 sau bản vá xác nhận đủ 10 hash trên: mỗi lần `rootCount=1`, `visibleRoots=1`, không overflow
  ngang. Đây là evidence browser/fake-host, không phải xác nhận AMIS WebView thật (O3/W4 vẫn mở).

### F32 — AI ingress/egress tin output provider quá mức và upload có thể đi thẳng sang Gemini · **P1 High / data-integrity + privacy — ĐÃ FIX (2026-09-02)**

- **Bằng chứng ban đầu:** `award-extract` từng dùng uploader audio để nhận mọi file và đưa PDF/ảnh raw thành `inlineData` cho Gemini; `event-extract` parse bảng tính nhưng gửi nguyên `parsed.text` (có thể gồm SĐT/email) ra ngoài. `genJSON()` chỉ `JSON.parse()` nên field lạ, enum sai, kiểu số sai hoặc chuỗi bất thường từ provider vẫn đi tiếp. Voice tự gán ngày hiện tại khi audio không nói ngày, làm dữ liệu lịch sử sai nhưng trông hợp lệ.
- **Resolution fail-closed:** chỉ Excel/CSV qua worker + signature/giới hạn mới được parse/redact rồi gửi; PDF/ảnh bị từ chối với hướng dẫn dán nội dung đã rà soát. Mọi text upload/web được redact và đóng khung `untrusted` để model không coi dữ liệu nguồn là mệnh lệnh. Gateway kiểm tra/prune JSON theo schema cục bộ, giới hạn output, chặn grounding URL không HTTPS/credential; enum `organizer_type` và event `mode` được ép, ngày không tồn tại bị bỏ trống để người dùng rà soát. Audio phải khớp MIME+đuôi+magic bytes, có consent rõ ràng, không tự suy diễn ngày; propose/confirm vẫn human-in-the-loop và server kiểm tra lại trước write.
- **Live evidence:** `npm run test:gemini:live` dùng dữ liệu tổng hợp vô hại, không in/lưu prompt, audio, response hoặc key. Lần chẩn đoán đầu phát hiện event `mode` drift; sau fix contract + prompt production, chạy lại **8/8 PASS**: award/event structured extraction, grounded source sanitation, text, image và **3/3** voice extraction độc lập. Artifact metadata-only, gitignored: `data/gemini-live-contract-last.json`.
- **Regression evidence:** SQLite `924 total / 916 pass / 8 skip`; MySQL `926 total / 925 pass / 1 skip`; `test:security` 6/6, `verify-g0.mjs`, mapping 150/150 và Vite build đều PASS. Các test mới `BR-AI-003b/c`, `BR-AI-010b/c`, `BR-AI-011b`, `R136 privacy/magic-byte`, `R139 BR-AI-018`, `R141 BR-AI-019` khóa output contract, consent, upload redaction và prompt-injection boundary.
- **Residual:** Gemini là probabilistic nên không được coi là authority nghiệp vụ. Những dữ liệu nhạy cảm thật vẫn phụ thuộc O8/Security-Legal; voice/AI chỉ tạo bản nháp, người dùng phải xác nhận và PolicyEngine/transaction vẫn là authority ghi dữ liệu.

## E. Điểm mạnh nên bảo toàn
- Mô hình nghiệp vụ PR phong phú, liên hệ nhiều thực thể.
- RBAC server-side + audit + per-user `sensitive_perms` (biểu cảm hơn role cứng).
- AI human-in-the-loop (đưa vào form để duyệt trước khi lưu).
- Excel AI pipeline có worker isolation, magic-byte check, redaction email/phone, giới hạn tài nguyên.
- Report tính số **deterministic** ở server rồi mới cho Gemini viết narrative (`monitor.js:281-306`) — mẫu chống AI bịa số, nên nhân rộng.
